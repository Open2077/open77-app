# OPEN//77 Hub execution ledger

Goal active. Complete H00–H15 from [implementation contract](community-hub-implementation.md); no production publication/deployment. Paid/deferred features excluded. No requirement is complete merely because a UI or API stub exists.

## Isolated workspaces

All four use branch `feat/community-hub`, created from the existing checkout HEAD without switching its branch.

| Checkout | Baseline | Original state preserved |
| --- | --- | --- |
| `../hub-app` | `8e8f5f7` | app/main; two planning documents copied into this checkout |
| `../hub-master` | `7735a2f` | master/main |
| `../hub-base` | `f8ae5471` | base/av-fix-merge; unrelated research changes left untouched |
| `../hub-launcher` | `013e11a` | launcher/fix/mod-menu-footer-and-solo-panel; untracked .claude left untouched |

## Acceptance status

- [ ] H00: baseline/environment/contract — worktrees ready; Docker 29.7.2 responds; pinned submodule ready; app dependencies installed; SSR preview path pending.
- [ ] H01: interface prototype — discovery/directory/detail and creator draft surfaces implemented; responsive CSS present, rendered browser review pending.
- [ ] H02: schema/domain/permissions/revisions — migrations through 36, project/release lifecycle, profiles, accepted membership/ownership, service isolation and quota overrides implemented/tested. Unpublished draft deletion and final acceptance audit remain open.
- [ ] H03: project/profile/release APIs — initial project create/edit/submit/review/public/private listing routes tested over HTTP; broader contracts still pending.
- [ ] H04: private file gateway/storage/jobs — real private upload, inspection, review/download and scoped Warden publishing proven; auxiliary-principal isolation and restored guards tested. Actual current-service outage/full-disk and final failure acceptance remain pending.
- [ ] H05: package/media inspection — shared bounded ZIP/preload inspector, media worker and real ClamAV integration tested; final corpus/acceptance audit pending.
- [ ] H06: review/report/revocation/admin
- [ ] H07: live publishing/download/directory
- [ ] H08: social/notifications — moderation/report inbox, votes, private saves, release subscriptions, threaded comments and durable notifications implemented/tested; broader abuse/load and browser validation remain pending.
- [ ] H09: metrics/search/SEO/accessibility — controlled 10,000-project load run passes stated local latency gates with documented host/payload limits; operational collector and actual app browser acceptance remain pending.
- [ ] H10: GitHub import
- [ ] H11: Warden browse/planner — real catalog-to-reviewed-install flow proven; final acceptance audit pending.
- [ ] H12: Warden install/update/rollback — transactional execution/recovery, maintenance and retention tested; real local install/uninstall/reinstall/update/committed rollback proven with latest configuration/user data preserved. Restart-required flow and game acceptance remain.
- [ ] H13: Warden creator connection/export — real scoped publishing, accepted-draft restart resume and lost-create/PUT-response recovery proven. Actual web draft browser journey and final acceptance audit remain.
- [ ] H14: launcher/docs — launcher entry implemented and tested; guides present, final cross-flow audit remains.
- [ ] H15: CI/regression/restore/release artifacts — four earlier local images validated; schema36 paired restore including security guards passed. Release inventory recorder and daily operations guide present; current images, telemetry/outage checks, final release candidate and integration gates remain.

H16 production rollout is outside authorization.

## Evidence and findings

- 2026-09-11: `open77_status`: no master on 8090, existing dev server on 11778, no game instance. Preserve that server; use distinct local ports for hub tests.
- Read Open77 testing skill. Game/runtime validation will use hub-base artifacts explicitly; configured MCP defaults point at base.
- Master pins base submodule `b26baffbd6ec0cb7320ea52b01c81e73b3bdfa52`. Local Git configuration resolves its URL to the base checkout; permit file transport only for the explicit initialization command. No global Git configuration changes.
- Found master schema version 11 and advisory-lock migration runner. Community migrations must be additive, restartable and tested against real MariaDB.

## Next work

Finish unpublished draft deletion, operational telemetry/guard validation and current-service outage checks. Complete actual app browser, GitHub-provider and restart/game acceptance, then the final release-candidate audit. Real update/rollback, interrupted publishing recovery, controlled load measurement and schema36 restore have evidence below; they do not substitute for the remaining journeys. Keep each H00–H15 gate pending until its complete acceptance criteria have direct evidence.

## Foundation checkpoint — 11 September 2026

Implemented in hub-master:

- Additive restartable migration 12: projects, revisions, slug reservations, members, profiles, releases/artifacts, dependency/media/upload/job storage, comments/interactions, outbox/notifications/reports/audit/import/idempotency tables. Metrics/delegation and final lifecycle constraints are not yet complete.
- Immutable project content snapshots; published reads retain the approved revision during edits/rejection. Draft edits invalidate old submitted review snapshots.
- Current verified active-account checks, member-only editing, admin review, per-account draft quota, atomic slug reservations and submission media/release prerequisites.
- Feature flags default off. Project public/private HTTP routes and structured errors; private contribution requests have per-account rate limiting. Added needed PATCH/PUT CORS verbs without changing allowed origins.
- Basic listing uses stable ID pagination; date/trending/tag/compatibility sorts remain H09 work, not implemented behavior.

Validation:

- `dotnet build Open77.Master.slnx -c Release --nologo`: passed, zero warnings/errors.
- Executable runner `dotnet tests/Open77.Master.Tests/bin/Release/net10.0/Open77.Master.Tests.dll --filter 'FullyQualifiedName~Community' --minimum-expected-tests 13 --report-trx --report-trx-filename community-api.trx`: 13/13 passed against real MariaDB.
- Full executable runner with `--report-trx --report-trx-filename hub-master-regression.trx`: 116/116 passed, zero skipped, 48 seconds. Reports live under the test project's ignored `bin/Release/net10.0/TestResults/`.
- Tests prove private/public separation, concurrent edit conflict, review invalidation, role/current-status enforcement, quota prerequisites, migration interruption retry, comment project FK/duplicate vote constraints, HTTP publication and cursor ownership binding.
- Earlier `dotnet test ... -- --filter-class` invocation ran zero tests and was discarded. SDK CLI `-- --help` failed; invoking the built test executable directly exposes the supported `--filter` syntax. No test failures were suppressed.
- `npm ci --no-audit --no-fund` completed in hub-app. npm reported one pending third-party postinstall (`unrs-resolver`); no blanket install-script approvals granted.
- No app interface files were changed in this checkpoint: an attempted combined replacement patch was rejected atomically. UI work is the next step.

Scope remains H00–H15 in full. No production changes, no game startup, no changes to the existing dev server.

## Interface/storage checkpoint — 11 September 2026

- hub-app: dynamic Community landing, searchable/category-filtered resource directory, approved resource details/metadata, creator dashboard, new/edit forms with real API save/submit, account gate, conflict feedback and unsaved-text warning. Uses existing design tokens. No fixture projects or activity counts are presented as real.
- Markdown rendering removes raw HTML, external tracking images, unsafe link schemes and credential-bearing URLs; formats code/lists and safe links. Dedicated attack-fixture check passes.
- Frontend is incomplete: media/gallery, releases/download panel, autosaved five-step wizard, profiles/social/moderation, advanced filters and responsive browser review remain required. Current cards show category fallback artwork until processed-media delivery is implemented.
- `npm run check` passed. Production build passed with `/community`, `/resources` and resource details confirmed dynamic. `node --experimental-strip-types scripts/check-community-markdown.mjs` passed (Node reports benign module-type autodetection warning).
- hub-master: immutable SHA-256 blob storage, bounded streamed writes, cleanup after cancellation/failure, collision verification and path/reparse-point checks. Durable queue has exclusive claims, lease recovery, stale completion rejection, backoff and maximum three attempts.
- Master build passed after stopping this task's local master, which had locked its build DLLs. Community test run: 21 passed, zero skipped; report `tests/Open77.Master.Tests/bin/Release/net10.0/TestResults/community-storage.trx`. Full regression was not rerun after these isolated storage additions; prior 116-test baseline remains recorded above.
- Created isolated Docker container `open77-hub-mariadb`, bound only to `127.0.0.1:13316`, with random local credentials in ignored `hub-master/src/Open77.Master/master.json`. Existing shared MariaDB and dev game server left untouched. Local hub master was started on 18090 and then stopped for rebuilding; DB container remains running.
- Automatic approval review rejected the local Next server startup on 3008 with only `blocked by policy`. No alternate launch was attempted to bypass that rejection. Browser/served validation remains pending; it is not implied by build success. Other backend implementation remains unblocked.

Next: upload grants/quota reservations, file gateway and inspection pipeline; finish browser validation when the server-start rejection is resolved. Goal remains active with all H00–H15 requirements intact.

## Upload/archive checkpoint — 11 September 2026

- Added independent `Open77.Community.Files` gateway. It verifies an existing schema without applying migrations or loading platform signing keys. Transfer authorization uses expiring upload-specific bearer grants; account session tokens are not accepted by this gateway.
- Master creates immutable SemVer release identities, reserves per-account upload slots/bytes transactionally, issues grants, exposes private status, and finalizes uploads into quarantine plus one durable inspection job. Completed transfers cannot overwrite bytes. Gateway bounds stream size, concurrent transfers and transfer duration; interrupted transfers reset for retry.
- Additive migration 13 allows separate artifact ownership records to reference the same immutable physical digest. Public visibility remains governed by individual artifact/release state.
- `dotnet build Open77.Master.slnx -c Release --nologo`: zero warnings/errors. Community tests: 26 passed, zero skipped (`community-uploads.trx`). Full master regression: 129 passed, zero skipped in 50 seconds (`hub-upload-regression.trx`). Reports are under the test project's ignored `bin/Release/net10.0/TestResults/`.
- Real HTTP integration tests use MariaDB and private temporary filesystem storage: grant isolation, retry after oversized data, single-use transfers, cross-account ownership, concurrent quota reservation, exact bytes, private visibility and idempotent job enqueue. Random test bytes deliberately remain quarantined; this does not claim successful package inspection.
- Added disposable ZIP staging with shared expanded-byte/entry budgets, portable paths, case/Unicode collision checks, link/device rejection, bounded streamed extraction and cleanup. Six attack/cleanup/budget tests pass (`community-zip-boundary.trx`). This container boundary is not complete package or malware validation; manifest/media/preload inspection and worker integration remain required.
- hub-base parser now has a package-root entry point using the same resource validation implementation, while installed resources still require matching directory names. Parser/preload/resource-subsystem selection: 46 passed, zero skipped (`hub-resource-parser.trx`).
- First base server build found the new worktree's native transport absent. Native source/pins match the existing base checkout (`git diff f8ae5471 -- server/native` empty); copied only its cached Release transport DLL into this worktree. Source/destination SHA-256: `06ef37f835e35b3704ebf5696c8cb9d56a561ea06bfff3b78cea3cb41abc11cf`. No identity/config/cache deletion. The subsequent server build passed; server regression is running.

- Base full server suite finished: 909 passed, five environment-dependent skips (native platform module and four database scenarios), one pre-existing `ShippedWebUiUsesCefSafeCustomDropdowns` failure. Failing test/Freeroam HTML unchanged from baseline `f8ae5471`; prior runbook documents this same failure. The full suite is not green; targeted Hub parser validation is green. No test was suppressed or changed to hide this failure.

Remaining: all unchecked acceptance gates, including worker inspection, download authorization, full creator/social/moderation UI, GitHub/Warden/launcher integrations and release validation. No production publication/deployment.

## Shared package parser checkpoint — 11 September 2026

- Base commit `d8b863ca` adds `ParsePackageRoot` without duplicating the parser or relaxing runtime directory validation. Master now pins that local commit. The dependency has not been pushed or published; release preparation must account for publishing compatible commits only when authorized.
- Started the `Open77.Community.Worker` project with manifest inspection, currently a library rather than a running queue consumer. It accepts root/wrapped resources and explicit bundles, records archive/install roots plus authoritative manifests, preserves distinct runtime versions, and rejects overlapping/missing roots, duplicate install names and unmapped non-documentation files.
- `open77-hub.json` schemaVersion/version/resourceRoots are checked against the submitted release. Dependency/config-template metadata still needs validation and persistence. Preload contents, forbidden-file/secret checks, image processing, worker lifecycle and atomic inspection-result persistence remain required before any artifact can leave quarantine.
- Master solution build passes with the new pinned resource dependency. Community selection: 38 passed, zero skipped (`community-package-manifests.trx`), including six new root/wrapper/bundle/parser cases. No public acceptance or runtime installation is inferred from these manifest-only tests.

## Media processing checkpoint — 11 September 2026

- Worker image processor accepts decoded JPEG/PNG/WebP, enforces 10 MiB/40 MP/20,000-pixel-side limits, rejects animation, applies all eight EXIF orientations and creates fresh WebP derivatives at maximum edges 480/1280/2048 without upscaling. Original encoded bytes/metadata are not reused for delivery.
- Pinned SkiaSharp and its minimal Linux native assets at 4.152.0. Sources: [maintainer package and MIT license](https://www.nuget.org/packages/SkiaSharp/4.152.0), [Linux assets](https://www.nuget.org/packages/SkiaSharp.NativeAssets.Linux.NoDependencies/4.152.0), [codec API](https://learn.microsoft.com/en-us/dotnet/api/skiasharp.skcodec). NuGet mapping explicitly includes this package family. Initial restore required reevaluating the existing test dependency lock; `dotnet restore ... --force-evaluate` succeeded without relaxing source or vulnerability policies.
- Master build passes, zero warnings/errors. Four native-codec tests pass (`community-images.trx`): all supported input formats, derivative sizing/decodability, removal of appended private data, all EXIF orientation quadrants, unchanged small-image size, cancellation, SVG/HTML/empty/oversized input and huge declared dimensions rejected before full decode. Native Linux codec execution and memory-limit/container tests remain pending.
- Image processing is not yet connected to persisted jobs/media rows or public delivery. H05 stays open, along with the remaining full-platform gates.

## Durable image worker checkpoint — 11 September 2026

- `Open77.Community.Worker` is now an executable generic host consuming image jobs. Package jobs remain queued for the package pipeline; manifest-only validation does not publish or accept packages.
- Worker verifies stored input digests, creates immutable derivatives and commits artifact/upload/media/job state in one lease-fenced transaction. Invalid media records a bounded rejection code. Expired/reclaimed workers cannot commit or reject another attempt's result. Operational failures use durable retry/backoff.
- Upload completion is idempotent after processing, acceptance or rejection. Private status returns the processed media ID/rejection code for the creator UI.
- Community selection: 46 passed, zero skipped (`community-image-worker.trx`). Full master regression after status integration: 149 passed, zero skipped (`hub-worker-regression.trx`, 62 seconds). An additional separate-process test then passed (`community-worker-process.trx`): launches the actual worker executable against MariaDB/private storage, observes processed media, stops only that process.
- A build started while the regression executable still held its DLL and emitted retry warnings; it completed after the regression released the file. Subsequent process test used the newly built test assembly. No running test was killed or silently bypassed.
- Added master `docs/community-services.md` with current configuration, process boundaries, recovery semantics and explicit remaining operations work. H04/H05 remain open: package inspection, public delivery, media UI, Linux/process-limit validation, dead-letter replay and orphan cleanup are not complete.

## Package content/preload checkpoint — 11 September 2026

- Package inspection now scans files for private runtime/identity/configuration material, database/log/install/native files, renamed native binaries, undeclared archive magic, LFS pointers and recognizable embedded credentials. Dynamic credential references yield bounded path/code review findings without matched values. This is not a claim that Lua is harmless or a substitute for malware signatures and moderation.
- Added declared ZIP/7z preload inspection. Both reuse the same path/link/collision/extraction boundary and cumulative outer/nested byte/entry budgets. Extracted preload files must satisfy the pinned server's inert `ModRootPolicy`, compiled directly from its source. Nested containers inside preloads are rejected. SharpCompress 1.0.0 matches the pinned base dependency.
- Added an authored real 7z fixture using Windows bsdtar 3.8.8. Its text file is deliberately not a game asset; no map/runtime acceptance is inferred. Initial test expected 56 expanded bytes; the actual fixture contains 58. Corrected the test expectation. Initial run: 55 passed/one assertion failure; corrected community run: 56 passed, zero skipped (`community-preloads-corrected.trx`, 26 seconds). Master build passes, zero warnings/errors.
- Tests cover native/private-file/LFS/secret rejection, secrets across streaming boundaries, template/dynamic credential review findings, root-level resource with ZIP preload, real 7z extraction, invalid mod roots/executable preload content, recursive containers and shared nested budgets.
- Package queue consumption, signature scanning, dependency/config-template metadata validation, immutable inspection persistence and release moderation/delivery are next. Full H00–H15 objective stays active; no production publication/deployment.

## Package worker/signature checkpoint — 11 September 2026

- Package worker now claims package jobs when configured with a private scratch root and internal ClamD endpoint. It verifies the stored digest, inspects resource/preload/content rules, requires an explicit clean signature result, then atomically records the immutable inspection snapshot and resource manifests while moving the release to `pending_review`. No release is published by technical validation.
- Additive restartable migration 14 adds `community_releases.inspection_json`. Acceptance remains fenced by active job owner/attempt/expiry and current upload/artifact/release states. Scanner outages retain quarantine and durable retry state.
- Added ClamD INSTREAM client with bounded bytes/replies/deadline, no uploaded-path commands, deterministic signature rejection and recorded scanner version. Added `ops/community/clamd.conf`: limits cover Hub archives and `AlertExceedsMax yes`/`AlertEncrypted yes` prevent incomplete scans being silently accepted. Signature freshness monitoring and exhaustive scanner-limit tests remain release work.
- Started dedicated local `open77-hub-clamav` on loopback 13310 with its own signature volume, 4 GiB memory and two CPUs. Pinned image digest `sha256:1fdfd24c6f0a0fb60788481487459a6d4eda8a9b448641594e04db8410d34422`; observed ClamAV 1.5.4, 3,642,760 signatures. Applied the checked-in scanner configuration and verified daemon readiness/effective limits. Existing MariaDB/dev server unchanged.
- Community selection against real local ClamAV: 58 passed, zero skipped (`community-package-worker.trx`). Includes clean ZIP acceptance into review, standard EICAR marker rejection and injected scanner outage retaining quarantine. Master build passes, zero warnings/errors.
- Full master run: 160 passed, one test-harness failure copying ClamD config to the disposable container (`hub-package-regression.trx`). Fixed the destination directory and cleanup on startup failure. The previously failing test then passed using its own real disposable scanner with the checked-in config (`community-disposable-scanner.trx`, 17 seconds). No test was skipped or suppressed; full suite has not been repeated after the harness-only correction.
- Remaining H05 work includes dependency/config-template metadata validation, stronger malicious/limit fixtures and real map/preload runtime scenarios. Release review/download APIs, remaining UI/social/profiles, GitHub/Warden/launcher and operations acceptance remain required. Goal active; production untouched.

## Release review API checkpoint — 11 September 2026

- Added public/private release lists with bounded cursor pagination, immutable artifact digest/size, runtime manifests and version metadata. Inspection snapshots are private to members/admins; public history includes published/revoked releases only. Resource rows load in one batched query per page.
- Added admin release approval/rejection/revocation routes with current-role checks, optimistic revision checks, inspection/artifact prerequisites, audit records and transactional outbox events. Technical acceptance still does not publish a release. Downloadable project approval now requires at least one reviewed, published release; approving a release alone does not expose a private project.
- Rejected replacement releases leave earlier published bytes and metadata intact. Revocation preserves release history and digest; download enforcement will use current state when that integration is added.
- Master build passes, zero warnings/errors. Community selection: 62 passed, zero skipped (`community-release-api.trx`, 30 seconds). Real MariaDB/ClamAV lifecycle tests cover publication prerequisites, stale/unauthorized decisions, private snapshot visibility, rejected replacements and revocation history. HTTP tests cover draft visibility, pagination and rejection of uninspected/admin-ineligible decisions. Cursor tests cover complete pagination and project/audience binding.
- Downloads/media delivery, review queues/UI, moderation/report workflows and the remaining H00–H15 integrations are still required. No production publication/deployment.

## Private download gateway checkpoint — 11 September 2026

- Added additive migration 15 for expiring delivery grants and merged completed byte ranges. Guest download requests require a currently approved downloadable project, published inspected release and accepted artifact. Every gateway use rechecks current state; revoked, suspended and expired grants cannot deliver bytes.
- Files now streams original ZIP attachments with HEAD, single/suffix byte ranges, If-Range, digest ETag, no-store/nosniff/referrer controls and independent bounded transfer concurrency. Framework URL logging is suppressed because delivery URLs carry short-lived query grants; proxy redaction remains part of production configuration work.
- Completion is recorded after successful body write/flush. Range unions count a delivery only once, including overlaps and retries. HEAD, unsatisfiable ranges and denied requests do not count. Visitor/day ranking deduplication, aggregates and failure reconciliation remain required.
- Separate master/gateway download flags default off. Master build passes, zero warnings/errors. Community selection: 64 passed, zero skipped (`community-downloads.trx`). After tightening grant lock ordering and adding suspension/expiry assertions, both download integration tests passed again (`community-download-authorization.trx`).
- Integration evidence traverses real HTTP upload, private storage, MariaDB, ClamAV inspection, release/project approval, guest grant creation and exact-byte/range delivery. Tests also prove grant invalidation after revocation, suspension/expiry, paused gateway behavior and once-only full-range accounting. Browser UI integration and interrupted-network/socket behavior still need their own validation.
- Goal remains active in full. Public media, publishing/download UI, profiles/social/moderation, GitHub/Warden/launcher and remaining H00–H15 gates are unfinished. Production untouched.

## Public release interface checkpoint — 11 September 2026

- Resource pages now load reviewed release metadata, with a cursor-paginated version-history route. Cards show changelog, author-declared tested builds, runtime resource requirements/permissions, installation, license and immutable SHA-256. Prereleases are explicitly expanded; revoked releases retain history without a download action.
- Download controls request real short-lived gateway grants and provide an explicit ZIP link, with expiry/error states. Grants remain in component memory; no file buffering or account-token forwarding to the file gateway. Current API/gateway authorization still decides availability at use time.
- `npm run check` passed (TypeScript and ESLint). Production `npm run build` passed against explicitly configured loopback API origins; resource/version routes remain dynamic. Browser delivery, responsive/accessibility review and end-to-end creator flow remain unvalidated; the earlier automatic approval rejection of local Next server startup remains recorded above.
- H07 and the overall H00–H15 goal remain open. No production publication/deployment.

## Creator ZIP upload recovery checkpoint — 11 September 2026

- Master now exposes private, paginated upload history with filename, state and bounded inspection results. Cursors bind project/account; responses omit grants and storage paths. Pending-upload restart rotates the scoped grant without extending expiry or quota. Current uploader/project authorization is checked; receiving, uploaded, processing and expired reservations cannot be restarted or overwritten.
- Creator resource editors now create immutable release versions, transfer ZIP files directly to Files with progress/cancellation, finalize stored uploads, and recover pending/processing/rejected state from the API. Processing polls back off and stop after a bounded number of checks; refresh remains explicit. Interrupted pending transfers can reselect the file and restart from byte zero. No account session is sent to Files and no ZIP is unpacked in the browser.
- Added recovery/rotation database tests and extended the real HTTP gateway test: old grants fail after replacement, new grants stream successfully, guest history is denied and processing state can be recovered. Community selection: 66 passed, zero skipped (`community-upload-recovery.trx`). Full master regression: 169 passed, zero skipped (`hub-upload-recovery-regression.trx`, 69 seconds). Master solution build passed with zero warnings/errors.
- App `npm run check` and production `npm run build` passed with loopback API origins. Initial lint caught render-time clock reads; expiry display now uses the last status-check timestamp. Status refresh preserves an active transfer component instead of aborting it as receiving state changes. Browser/CORS/socket interruption behavior is not yet validated; the previously rejected local Next startup remains an outstanding validation constraint.
- These controls are an incremental creator integration, not the completed five-step autosave/media/review wizard. Media delivery/attachment, actionable review screens, profiles/social, GitHub/Warden/launcher and remaining acceptance gates remain required. Goal active; production untouched.

## Approved screenshot delivery checkpoint — 11 September 2026

- Added public derivative metadata and GET/HEAD image delivery. Both require accepted processed media referenced by the current approved project revision. Originals and unapproved/unattached media cannot be delivered. Draft edits preserve the approved image; approved removal and suspension deny new requests, while archival preserves delivery.
- Files media has its own default-off flag, 16 transfer slots and a 60-second deadline. It serves only worker-created WebP with no-store/nosniff/no-referrer and sandboxed CSP. No public original-image route or unrestricted storage path exists.
- App resource cards and detail galleries now load real derivative URLs/dimensions. Images bypass Next optimization to preserve gateway authorization/takedown checks; frames reserve layout space and support captions/alt text. Browser rendering, client failure states, CSP rollout and load/accessibility verification remain pending.
- Master build passes with zero warnings/errors. Community suite: 67 passed, zero skipped (`community-media-api.trx`). Extended gateway takedown test passed again (`community-media-takedown.trx`): actual image upload, native processing, approval, all three WebP variants, HEAD, metadata privacy, original rejection, paused service, archival, suspension and approved removal. App TypeScript/lint and production build pass with loopback API origins.
- Next required publishing work includes private creator/reviewer image previews and image attachment/reordering, then the complete autosave wizard and review screens. All remaining H00–H15 scope stays active; no production publication/deployment.

## Private previews and creator screenshots checkpoint — 11 September 2026

- Migration 16 adds expiring image-preview grants. Current verified members/admins can request a five-minute grant for a processed image. Gateway use rechecks grant/expiry, accepted artifact, current account status and membership/admin role. Preview URLs carry only a scoped grant; account sessions and original images are not delivered through this route.
- Creator editor now uploads and recovers images, polls processing with bounded backoff, attaches up to nine screenshots, previews processed derivatives privately, edits alt text/captions and reorders/removes draft attachments. The first image is the cover. Changes still use the existing project revision/save/review boundary.
- Fixed an editor save race: newer content edits made while a request is in flight remain dirty, and initial creation does not navigate away while newer edits remain unsaved. Full autosave/wizard/concurrency recovery and browser validation are still required.
- Initial community run: 67 passed, one failed because the migration recovery test still expected schema 15. Updated it for migration 16 and checked the preview table exists. Full master regression then passed: 171 tests, zero skipped (`hub-private-media-regression.trx`, 75 seconds). Coverage includes private HTTP image delivery, guest denial, wrong grant/original denial, expiry, account suspension, membership removal and admin role removal. Master build passed with zero warnings/errors.
- App TypeScript/lint and production build passed with loopback API origins. Initial typecheck caught an unchecked reorder index; the reorder now guards both entries. No browser behavior is claimed from these checks. Expired preview cleanup, CSP/deployment configuration and actual reviewer screens remain open, along with the remaining H00–H15 integrations. Production untouched; goal active.

## Moderation queue and decision screen checkpoint — 11 September 2026

- Added current-admin project/release review queues with bounded oldest-first pagination and account/queue-bound cursors. Project queues use only submitted current revisions; release queues require accepted artifacts and immutable inspection snapshots. Suspended/archived projects are excluded.
- Added exact release review lookup, including digest, metadata, resource manifests and inspection evidence. Existing expected-revision decision endpoints remain authoritative; stale decisions fail and reviewed entries leave the queue.
- Added `/admin/community` to the existing operations workspace. Reviewers inspect project text/links, private processed screenshots, release metadata/hash/inspection, then record approval/rejection with a reason and an explicit reviewed-revision confirmation. Submitted content is displayed as escaped text; no untrusted raw HTML is rendered. Queue refresh participates in existing admin activity UI.
- Master solution build passed with zero warnings/errors. Community selection: 70 passed, zero skipped (`community-review-screen-api.trx`, 43 seconds). Tests cover guest/non-admin denial, current-role removal, pagination/cursor binding, draft edits invalidating old review, uninspected release exclusion, release snapshot access and removal after decisions. App TypeScript/lint and production build pass with loopback API origins.
- H06 remains open: private package inspection downloads, report/takedown workflows, review history/appeals and browser validation still need implementation/evidence. H07 full autosave wizard and all remaining profiles/social/GitHub/Warden/launcher/operations gates remain required. Goal active; production untouched.

## Private package review download checkpoint — 11 September 2026

- Migration 17 adds separate review-delivery grants. Current verified admins request an inspected artifact at an exact release revision. Grants expire after five minutes, recheck current administrator status and artifact/revision on every use, and become invalid after a review/revocation changes that revision. Issuance is audited without recording the grant token.
- Master/gateway review-download flags default off independently of public downloads. The private route streams original ZIP attachments and ranges through the shared delivery implementation, but never contributes to public delivery rows/ranges/counts. Public and private grants are not interchangeable.
- Admin release review now exposes the private ZIP inspection link, with expiry/error handling and a visible artifact digest. Private project state does not prevent authorized inspection, and no uninspected/quarantined package gains a download path.
- Master build passes with zero warnings/errors. Community selection: 72 passed, zero skipped (`community-review-download.trx`, 40 seconds). Real HTTP evidence covers exact original bytes before publication, ranges, guest/creator denial, public-route denial, independent pause, no public counts and invalidation after decision. Database tests cover role removal, suspension, expiry, stale revision and audit issuance. Migration recovery expects schema 17 and verifies the new table.
- App TypeScript/lint and production build pass with loopback API origins. Browser validation and expired-grant cleanup remain required. Report/takedown/review-history workflows, full creator wizard and all remaining H00–H15 integrations stay open. Goal active; production untouched.

## Community reports checkpoint — 11 September 2026

- Added verified-member reports for public projects, inspected public releases and visible comments, with bounded reasons, concurrent open-report deduplication and a rolling ten-per-day quota. Duplicate reports retain the original ID/text. Private targets, hidden comments and uninspected releases are rejected.
- Added current-admin report queues with account/filter-bound pagination and open/action-taken/dismissed states. Resolving an open report records the admin, private audit notes and a generic reporter outcome in one transaction. Concurrent/stale resolution fails; private investigation notes are excluded from the outbox payload. Notification delivery itself is still pending.
- App project/release pages now expose report forms. The admin report queue supports content inspection, private images/release ZIPs and resolution notes. Canonical moderation routes now match the implementation plan: `/admin/resources` and `/admin/resources/reports`; the earlier `/admin/community` route redirects.
- Master build passed with zero warnings/errors. Community selection: 75 passed, zero skipped (`community-reports.trx`, 47 seconds). Evidence includes concurrent dedup/resolution, private target and role denial, quota, comment visibility, pagination, HTTP submission/resolution, audit/outbox atomicity and private-note exclusion. App TypeScript/lint and production build passed with loopback API origins; an initial unused import was removed after typecheck caught it.
- H06 remains open for suspension/restoration/revocation controls, pending-report assignment, review history/appeals and browser validation. Notification delivery, the full creator wizard and remaining H00–H15 integrations remain required. Goal active; production untouched.

## Project takedown and restoration checkpoint — 11 September 2026

- Migration 18 adds a separate moderation revision and recorded pre-suspension state. Current-admin suspension/restoration is atomic, rejects stale moderation cycles, records audit/reason and enqueues owner notification. Restoration preserves draft/submitted/changes-requested/published/archived state rather than assuming publication.
- Added admin controls for project suspension/restoration and exact-revision release revocation. `/admin/resources/[id]` provides private project inspection and paginated release history so suspended projects remain manageable outside review queues. Review/report screens link to it; report inspection also exposes the actions directly.
- Community selection: 77 passed, zero skipped (`community-project-moderation.trx`, 61 seconds). Tests cover all prior states, creator edit blocking, stale repeated cycles, audit/outbox records, role denial and real HTTP delivery interruption/restoration with unchanged ZIP bytes/content revision. Master build passes with zero warnings/errors; migration recovery expects schema 18.
- App TypeScript/lint and production build pass with loopback API origins. Initial typecheck caught rendering an unknown inspection value as a React node; the display now checks for null explicitly. Browser interaction/confirmation/accessibility is still unverified under the previously recorded local Next startup rejection.
- Remaining moderation work includes pending-report assignment, review history/appeals and browser validation. Notification delivery, complete creator wizard, profiles/social, GitHub/Warden/launcher and remaining H00–H15 gates stay active. Production untouched.

## Assignment and decision-history checkpoint — 11 September 2026

- Migration 19 adds report revisions. Admins can claim, release or explicitly take over an open report with an audited private note. Revision checks reject stale assignments; resolution requires the displayed revision and refuses reports owned by another moderator. Assignment and resolution increment the report revision.
- Added paginated project/report activity APIs. Current project members see creator-facing moderation decisions and reasons; internal report notes and staff display names are excluded. Admins see full project audit and private report history. Cursors bind target/account/current audience, and access is rechecked on each request.
- Creator editor now opens decision history for rejected/suspended projects. Admin project/report views expose lazy-loaded audit history and report assignment controls. History is available before notification delivery; report resolution requests now include their expected revision.
- Community selection: 78 passed, zero skipped (`community-history-assignment.trx`, 40 seconds). Full master regression: 181 passed, zero skipped (`hub-history-assignment-regression.trx`, 82 seconds). Evidence covers takeover/release/stale resolution, role denial, creator/private history separation, audience-bound cursors and HTTP history/assignment flows. Master solution build passes with zero warnings/errors; migration recovery expects schema 19.
- App TypeScript/lint and production build pass with loopback API origins. Browser validation remains unproven under the recorded startup rejection. An appeal route, notification delivery, complete publishing wizard and remaining H00–H15 integrations are still required. Goal active; production untouched.

## Notification delivery and inbox checkpoint — 11 September 2026

- Migration 20 adds bounded invalid-event state to the outbox. A worker consumer now delivers review decisions, revocations, suspension/restoration and generic report outcomes into account notifications. Transactional row claims and recipient/event uniqueness prevent duplicate delivery on concurrent workers or replay. Invalid supported events are isolated; unknown kinds stay pending. Transient database failure rolls back for retry.
- The dispatcher projects only approved message fields. Report investigation notes never reach recipients; internal project links still require current access. Added account-bound all/unread pagination and idempotent read timestamps, with session, active-account and ownership enforcement.
- Added `/account/notifications` and Hub navigation. The inbox provides read controls, unread filtering, bounded requests, refresh, pagination and creation links. Notification state resets across accounts and filters. Subscription controls/events and browser interaction evidence are still pending.
- Master build passed with zero warnings/errors. Initial targeted tests: 3 passed (`community-notifications-targeted.trx`), including a separate worker executable delivering a queued decision against real MariaDB. Full master regression: 184 passed, zero failed/skipped (`hub-notifications-regression.trx`, 120 seconds). Coverage includes concurrent/replayed events, poison isolation, private-note exclusion, account/filter cursors, cross-account read denial and real HTTP persisted read state.
- App `npm run check` and production `npm run build` passed with loopback API origins; the new inbox route is generated. Browser validation remains pending under the recorded automatic approval rejection of local Next startup. Administrative dead-letter tooling, appeals, the complete publishing wizard and all remaining H00–H15 integrations remain required. Goal active; no production publication/deployment.

## Decision appeals checkpoint — 11 September 2026

- Added creator appeal submission/status for exact immutable rejection, revocation and suspension audit entries. Current active verified membership is required even for suspended projects. Approvals/internal audit entries and unrelated accounts are denied. Concurrent/repeated submissions return the existing receipt, including after closure, and share the report quota.
- Appeals enter the existing assigned moderation queue with their original decision text and project link. Migration 21 adds a creator-facing response separate from private investigation notes. Resolution requires that response and atomically stores/audits it and enqueues an appeal notification. Closing an appeal never implicitly restores or publishes content.
- Creator decision history now offers an appeal form and persistent status/response. Admin queue supports inspection, assignment, resolution and the separately labelled creator response, including on closed entries. This copy remains draft pending project-owner review before public acceptance.
- Master build passed with zero warnings/errors. Targeted database test passed (`community-appeals-targeted.trx`). Community regression: 84 passed, zero failed/skipped (`community-appeals.trx`, 58 seconds), covering all supported decision kinds, ownership, concurrent deduplication, private audit denial, response validation, HTTP submission/resolution/status, notification privacy and unchanged content availability. Migration restart coverage now expects schema 21.
- App production build passed with loopback origins. Browser journeys remain pending under the previously recorded automatic approval rejection of local Next startup. H06 still requires broader comment moderation/editorial controls and browser validation; the full publishing wizard, profiles/social, GitHub/Warden/launcher and remaining H00–H15 gates stay active. Production untouched.

## Creator profile and author-page checkpoint — 11 September 2026

- Migration 22 adds profile revisions/timestamps and permanent handle reservations. Current active verified accounts can create/edit public handles, biographies and up to five labelled HTTPS links. Handles normalize to lowercase; official/staff prefixes are reserved. Concurrent claims have one winner, stale edits fail, and old handles stay bound to their account across renames.
- Public creator reads include only public profile fields and approved published/archived creations. Pagination binds the creator and works across canonical/old handles. Draft edits and unpublished projects stay private; suspended accounts have no public profile. Project DTOs now carry the current public author handle without extra client requests.
- Added `/account/profile` and `/creators/[handle]`, public bio/links, canonical redirects, paginated creation cards, and author links on cards/resource pages. Profile writes respect contribution pause. Avatar upload/processing/delivery and profile moderation remain required; this is not completion of the profile gate.
- Master build passed with zero warnings/errors. Targeted profile tests: 2 passed (`community-profiles-targeted.trx`). Community regression: 87 passed, zero failed/skipped (`community-profiles.trx`, 69 seconds). Evidence includes concurrent claims, stale edits, reserved/unsafe values, old-handle protection, approved-only listing, cross-creator cursor denial, account suspension and HTTP session/privacy behavior. Migration restart coverage expects schema 22.
- App TypeScript/lint and production build passed with loopback API origins. Hub links now wrap as account navigation grows; rendered responsiveness remains unverified under the prior automatic approval rejection of local Next startup. Avatar integration, full publishing wizard, social/GitHub/Warden/launcher and remaining H00–H15 gates stay active. Production untouched.

## Profile avatar integration checkpoint — 11 September 2026

- Migration 23 allows profile-owned images without creating a project. Avatar reservation/history use dedicated profile routes but share scoped grants, quotas, streaming, expiry/restart, durable inspection, native image limits and metadata-free WebP derivatives. Upload/media contracts now express nullable project IDs; package uploads still require a project.
- Profile revisions include avatar selection/removal. Only the account's accepted profile image may be selected; unrelated account images and project media are ineligible. Inspection alone leaves it private. Public media requires current profile selection and an active verified owner; removal and account suspension deny subsequent delivery. Private previews remain owner-bound and originals stay inaccessible.
- Shared image controls now support avatar upload/recovery, single selection, preview and removal. Profile save applies that selection explicitly; the public creator page renders its processed image. No fake project, remote avatar URL or direct public original is used.
- Master build passed with zero warnings/errors after updating project-only test fixtures for nullable upload IDs. Targeted avatar test passed (`community-avatar-targeted.trx`). Full master regression: 192 passed, zero failed/skipped (`hub-avatar-regression.trx`, 96 seconds). Evidence includes real gateway upload and native image processing, private-before-selection behavior, owned history/preview, grant rotation, profile selection, decoded WebP delivery, original denial, removal and suspension.
- App TypeScript/lint and production build passed with loopback API origins. Browser validation and dedicated profile moderation remain open alongside the complete publishing wizard, social/GitHub/Warden/launcher and remaining H00–H15 gates. Documented schema-23 rollback compatibility: retain binaries that understand nullable project associations once avatar rows exist. Production untouched; goal active.

## Votes, saves and release subscriptions checkpoint — 11 September 2026

- Added independent retry-safe PUT/DELETE interaction APIs, bounded account-only project-state reads, private saved/subscribed lists and public eligible upvote counts. Owners/maintainers cannot self-vote; authors may save/follow their work. Removing preferences still works after content suspension and while contributions are paused. Unavailable saved projects expose only a removable placeholder.
- Approval transactions enqueue one availability event per immutable public release. The worker fans out to current eligible subscribers in bounded batches, atomically preserving its recipient cursor and inserted notifications. Replays do not duplicate recipient/event records; unfollow and release unavailability are rechecked. Followers added after an event do not receive that queued historical release.
- Connected resource vote/save/follow controls, card counts, `/account/saved`, `/account/subscriptions` and inbox subscription management. State and counts refresh from real APIs after mutations; errors require refresh rather than assuming a write failed. Private lists reset across accounts and preserve removal controls for unavailable content.
- Master build passed with zero warnings/errors. Targeted interactions: 2 passed (`community-interactions-targeted.trx`). Community regression: 93 passed, zero failed/skipped (`community-social.trx`, 82 seconds). Extended separate-worker executable test passed (`community-subscription-worker.trx`, 8 seconds), delivering both moderation and release notifications through real MariaDB; its package was inspected using the real scanner. Coverage includes concurrent duplicate votes, self-vote denial, private state/cursors, suspended-account counts, HTTP retries, fanout resume/replay, unfollow, late subscriptions and pre-delivery revocation.
- App TypeScript/lint and production build passed with loopback origins. Browser interaction remains pending under the recorded automatic approval rejection of local Next startup. Comments/replies, broader abuse/load/ranking checks, complete publishing wizard, GitHub/Warden/launcher and remaining H00–H15 work stay required. Goal active; production untouched.

## Threaded discussion checkpoint — 11 September 2026

- Added one-level comments/replies with account-scoped retry keys, bounded pagination, optimistic edits, author deletion, tombstones and posting quotas. Project owners/maintainers can pin or resolve root threads. Current administrators can inspect hidden comments and hide/restore with audited reasons; deleted text cannot be restored. Archived projects remain readable and suspended/private projects deny public discussion reads.
- Migration 24 adds private thread subscriptions. Reply notifications use resumable transactional batches, current eligibility/opt-out checks and recipient/thread/five-minute deduplication. They contain no comment body. Moderator decisions separately notify the author with the creator-facing reason. The separate worker executable test now delivers image processing, release and reply notifications against real services.
- Added discussion routes, focused thread links, reply pagination/composers, edit/delete/report/marker controls and administrative comment inspection in report review. Text is escaped; failed requests retain composer text and retry identity. Follow state is bound to the current account. Browser navigation, authentication recovery, responsiveness and maintainer UI remain acceptance work.
- Master build passed with zero warnings/errors. First full regression ran 200 tests: 199 passed and one exposed a notification JSON type mismatch. MariaDB JSON_OBJECT emits the hidden flag as a boolean; the dispatcher now reads a boolean. The corrected community regression passed all 97 tests with zero failures/skips (`community-discussion-fixed.trx`), including the previously failing HTTP/moderation flow and separate worker. The earlier failing result remains recorded in `hub-discussion-regression.trx`.
- App TypeScript/lint passed after the final changes. The final production build generated `.next/BUILD_ID` at 10:52:07 local time; its process exit output was lost during context compaction, so this checkpoint does not claim a separately observed final exit status. A prior discussion build passed. No browser acceptance is claimed under the recorded automatic approval rejection of local Next startup.
- Full publishing wizard, remaining moderation/abuse/performance work, GitHub/Warden/launcher and H00–H15 integration gates remain open. Production untouched; goal active.

## Publishing flow and autosave checkpoint — 11 September 2026

- Reorganized the creator editor into Basics, Showcase, Release, Validation, and Preview/submit steps. Steps preserve mounted media/release controls during ordinary navigation. Added tags/video fields, creator profile link, private processed-image preview and the same safe Markdown renderer used by public pages. Showcase projects do not require a ZIP. Package validation codes now include corrective guidance and inspected manifest requirements.
- Existing drafts autosave after 1.5 seconds of inactivity, serialize writes and retain edits made during an in-flight request. Errors pause automatic retries. Initial draft creation explicitly reserves its permanent address. Token refresh no longer reloads over edited content. Signing out retains the project editor state in this tab, hides it from a different account, and requires an explicit discard to switch editor accounts.
- Concurrent revisions offer a three-way comparison against the last saved snapshot. Independent field changes combine; overlapping fields require explicit choices. Galleries/tags remain whole-field choices. The result saves against the fetched revision, so a further concurrent update still conflicts. Added unsaved-change warnings for page unload and ordinary link navigation.
- Four standalone merge behavior tests passed (`node --test scripts/test-community-draft-merge.mjs`), covering independent changes, overlapping edits, identical retries, gallery removal/reordering and optional values. App TypeScript/lint passed and the production build exited successfully with loopback API origins. A final documentation-link correction uses the existing `/docs/server-resources` route; TypeScript/lint passed afterward.
- This remains an intermediate H07 checkpoint: release form inputs need durable draft storage, package preview needs complete file inventory, consent needs an API/audit record, and final preview must share the complete public presentation. Browser history/Back, auth recovery for nested upload/release forms, conflict interactions, responsive layout and accessibility are unverified under the recorded local Next startup rejection. No H07 acceptance gate is marked complete. All remaining H00–H15 integrations remain active; production untouched.

## Durable unfinished release forms checkpoint — 11 September 2026

- Schema 25 stores bounded unfinished release form text per project/account, separate from immutable versions and public revisions. Current verified membership is rechecked; another maintainer has a separate draft, administrators cannot inspect another account's form, and membership removal denies retained rows. Suspended/archived projects allow recovery reads but block writes.
- Added private GET/PUT release-editor APIs with optimistic revisions. Identical retries return the existing revision; different stale edits conflict. Incomplete version/build text is accepted only as a form draft, while final version creation retains strict validation. Saving a form never creates/publishes a release.
- The release form restores its server draft, autosaves after inactivity, serializes saves, retains newer in-flight edits and pauses on failures. Conflicts offer explicit comparison/use-saved/replace-with-mine controls. Immutable version creation flushes outstanding draft text first and disables editing during that action.
- The first targeted test found returned sub-millisecond timestamps differed from persisted timestamps on retry; the writer now returns millisecond precision. Corrected targeted test passed (`community-release-editor-fixed.trx`). Master build passed with zero warnings/errors. Community regression: 99 passed, zero failed/skipped (`community-release-editor.trx`, 78 seconds), including restart, access removal, admin/maintainer isolation, competing edits, bounds and real HTTP/no-store behavior. App TypeScript/lint and production build passed with loopback API origins.
- Browser journeys, unsent nested form state across account changes, complete package inventories, audited distribution confirmation and exact public preview remain H07 work. This does not close H07 or the broader H00–H15 goal. Production untouched.

## Recorded distribution confirmation checkpoint — 11 September 2026

- Project submission and immutable version creation now require an explicit `distributionRightsConfirmed` boolean. Missing/false values are rejected without advancing the project or creating a release. Autosaves do not assert consent. Web confirmation starts unchecked and changes invalidate it.
- Successful transactions append the statement text/version, actor, timestamp and exact project revision or release ID to the audit log. Failed transactions leave no confirmation event. This records the contributor's assertion; it does not verify ownership. Historical rows are not assigned invented confirmation records. Matching web/master write contracts must ship together.
- Master build passed with zero warnings/errors. Full master regression: 204 passed, zero failed/skipped (`hub-publishing-regression.trx`, 138 seconds), including new missing/false confirmation, target binding and transaction rollback tests. Existing moderation, notifications, uploads, real scanner/worker and account tests remained green. App TypeScript/lint and production build passed with loopback API origins.
- Browser validation, complete package file inventories, remaining publishing recovery cases, exact public preview and all remaining H00–H15 integrations are still required. Production untouched; goal active.

## Complete ZIP file inventory checkpoint — 11 September 2026

- Schema 26 stores ordered outer ZIP file paths, exact uncompressed sizes and SHA-256 hashes. The worker computes inventories after resource/content/preload checks and commits them with the accepted artifact and inspection snapshot under the existing job lease. Inserts use batches of 100; the existing 10,000-entry/500 MiB limits remain. Original ZIP bytes are unchanged. Nested preload archives are listed as files, with their contents separately validated.
- Added public and private paginated inventory APIs. Public visibility requires an accepted published/revoked release on a public project; private reads recheck current verified membership/admin access. Cursors bind release/account. Suspended projects deny public reads, and unfinished or older inspections without inventories return an explicit unavailable result. File listing does not count downloads or expose blob paths/source bytes.
- Creator releases, public versions and both moderator review views now expose on-demand file tables with bounded next/previous pages. The final wizard step includes release details and file inventories with creation/upload controls hidden; mounted release form state remains intact while switching steps.
- Master build passed with zero warnings/errors. A real-scanner 207-file test verified all paths/sizes/hashes across three pages and database insertion batches, plus visibility/cursor/legacy behavior (`community-file-inventory-targeted.trx`). Community regression: 103 passed, zero failed/skipped (`community-file-inventory.trx`, 89 seconds), including new HTTP private/public inventory checks and zero counted deliveries. App TypeScript/lint and production build passed with loopback API origins, including the final preview adjustment.
- Browser interaction/focus/responsiveness, 10,000-file performance, legacy fixture reinspection/replacement and the remaining publishing/auth recovery cases stay open. No historical artifact was silently rewritten. All remaining H00–H15 integrations remain active; production untouched.

## Nested editor authentication recovery checkpoint — 11 September 2026

- The account-bound creator subtree now uses React Activity to retain nested release/media control state while the account is unavailable, with effects cleaned up while hidden and restored after same-account authentication. This uses the installed React API, checked against its local type export and [React Activity documentation](https://react.dev/reference/react/Activity). Explicitly switching editor accounts still discards the prior subtree.
- Removed the inactive editor's early null render that had unmounted nested form state. Release forms also remain mounted when switching a project between resource and showcase. Project navigation/unload warnings now include unsent release text; the outer account gate retains an unload warning while reauthentication hides the editor.
- App TypeScript/lint and production build passed with loopback API origins. These checks do not establish browser behavior: account switching, interrupted transfers, Activity effect cleanup, Back navigation and visible-state isolation still require browser evidence under the previously recorded Next startup rejection. Goal active; production untouched.

## Owner archive/reopen checkpoint — 11 September 2026

- Added owner-only lifecycle reads and archive/reopen transitions with current verified ownership, expected content and lifecycle revisions, reasons and atomic audit history. Archive/reopen shares the moderation revision counter so stale cycles and moderator suspension changes cannot be bypassed. Drafts cannot be archived; suspended projects cannot be reopened by owners. Archival remains available during contribution pause; reopening is paused.
- Archival preserves the approved page, release bytes and downloads while freezing editing and new comments. Unapproved draft edits remain private across reopening. Creator controls fetch current lifecycle state before confirmation, allow reopening for unsaved-text recovery and lock archived/suspended editing. Public pages display an archive notice. The creations dashboard now shows lifecycle state and has bounded pagination instead of stopping after its first API page.
- The initial targeted fixture was a Showcase, which correctly denied package downloads; the fixture now creates a downloadable Resource and approves its release first. Corrected targeted test passed (`community-archive-fixed.trx`). Master build passed with zero warnings/errors. Community regression: 105 passed, zero failed/skipped (`community-owner-archive.trx`, 79 seconds), covering permissions, stale cycles/content, unchanged approved content, suspension interactions, audit history and real HTTP/gateway delivery of the unchanged archived ZIP.
- App TypeScript/lint and production build passed with loopback API origins after the dashboard changes. Ranking exclusion, browser confirmation/auth recovery and member invitation/ownership workflows remain required alongside remaining H00–H15 gates. Production untouched; goal active.

## External showcase videos checkpoint — 11 September 2026

- Connected stored video links to both public resource pages and private draft previews. YouTube/Vimeo players load only after an explicit click; autoplay remains off, controls/fullscreen are available and the direct source link stays visible if embedding fails. Closing a player removes its iframe. No remote thumbnail or player request is made by this component before loading it.
- Player URLs are built from recognized IDs on fixed provider origins; author-supplied autoplay/origin/background parameters are not forwarded. YouTube watch/short/live/short-link forms and direct Vimeo IDs/unlisted hashes are supported. Other valid provider pages retain a direct link. Unsafe schemes, credentials, spoofed hosts, control characters and nonstandard ports are rejected by the preview parser.
- Four parser tests passed (`node --test scripts/test-community-video.mjs`); app TypeScript/lint and production build passed with loopback API origins. Implementation references: [YouTube player parameters](https://developers.google.com/youtube/player_parameters), [YouTube privacy-enhanced embeds](https://support.google.com/youtube/answer/171780?hl=en) and [Vimeo player parameters](https://help.vimeo.com/hc/en-us/articles/12426260232977-About-Player-Parameters). These references establish URL/parameter behavior, not browser acceptance for this app.
- Live provider playback, iframe/host headers, mobile sizing, keyboard focus and provider failure journeys remain unverified under the recorded local Next startup rejection. Member invitations/ownership workflows and all remaining H00–H15 integrations remain required. Goal active; production untouched.

## Accepted membership and ownership checkpoint — 11 September 2026

- Migration 27 adds membership generation IDs and private expiring invitations. Owners invite public handles; no access is granted until the verified recipient accepts. Existing pending requests are deduplicated, daily/project/member quotas are enforced, cursors bind the account/project, and reciprocal requests lock accounts in stable order. Invitations retain the title shared at creation without disclosing later private draft edits.
- Ownership can be offered only to a current maintainer. Acceptance atomically changes exactly one owner, keeps the former owner as maintainer, rotates both membership generations, advances lifecycle revision, and revokes other pending invitations. Current owners can remove maintainers; maintainers can leave. Stale removal cannot remove a newly rejoined membership. Current private API authorization denies removed members.
- Added account invitation review with explicit acceptance/decline confirmation and paginated history; dedicated project member management with invite/revoke/remove/leave/ownership controls. Editor navigation uses its existing unsaved-change guard. Generic transactional inbox events link to invitations without embedding private project titles. Discussion pin/resolve controls now use authenticated current membership instead of owner-only UI checks.
- Initial migration tests rejected a MariaDB generated concatenated CHAR key; replaced it with a nullable numeric pending marker in a composite unique index. Targeted tests then passed. The first HTTP test used PUT for the existing PATCH profile contract; corrected the fixture. Master build passed with zero warnings/errors. Community regression: 109 passed, zero failed/skipped (`community-membership-regression.trx`, 96 seconds), including HTTP acceptance/transfer/removal, quota, expiry, concurrent dedup/reciprocal requests, stale generations, private-title isolation, audit and notification delivery.
- App TypeScript/lint and production build passed, including both new account routes. Browser confirmation/focus/auth recovery, full cross-service acceptance and remaining H00–H15 gates remain open under the previously recorded local Next startup rejection. No production publication/deployment; goal active.

## Bounded notification read-all checkpoint — 11 September 2026

- Added a verified-account read-all API with an explicit past timestamp cutoff, at most 500 unread rows per transaction, and a response indicating whether more matching rows remain. Retries preserve existing read timestamps. Newer notifications and other accounts are excluded; missing/future cutoffs fail validation.
- Inbox confirmation uses the newest item on the displayed page as its fixed cutoff and explains that older pages are included. Large inboxes can continue using the same cutoff without advancing it; refresh or cancel clears the pending choice. No external messages are sent.
- Master build passed with zero warnings/errors. Two new real-MariaDB/HTTP tests passed (`community-read-all.trx`, 12 seconds), proving the 501-row boundary, replay, account isolation, newer timestamps, missing/future input and guest denial. App TypeScript/lint passed after correcting strict optional array access. Prior 109-test community regression remains the immediately preceding baseline; no full regression was claimed for this isolated addition.
- Browser focus and race journeys remain part of outstanding H08/H15 acceptance. Goal active; production untouched.

## Delivery totals and daily view metrics checkpoint — 11 September 2026

- Schema 28 adds aggregate project counts, daily random HMAC keys and temporary per-project visitor dedup rows. Public project/list responses and pages expose actual views/downloads. A visible-page client POST records views; server rendering/prefetch reads do not. No browser analytics identifier is created. Normalized network addresses are hashed per project/day and never stored raw; shared networks can undercount people.
- File delivery completion increments totals atomically once per delivery ID, including merged byte ranges; grants and failed partial transfers do not count. Existing completed rows are backfilled. Repeated successful deliveries count in totals but eligible download attribution is capped per project/day/hash. Signed-in current members and inactive/unverified accounts are excluded from attribution; anonymous self-activity cannot be conclusively identified. Review downloads remain separate.
- Metrics collection defaults off. A dedicated exact-IP trusted-proxy list handles forwarded addresses only from configured peers, walking the chain to its first untrusted hop; arbitrary direct-client forwarding is ignored. Known automation/preview user agents are excluded. This leaves global auth/rate-limit proxy behavior unchanged. Actual deployment proxy configuration remains unverified.
- Worker maintenance prunes bounded batches of dedup rows/keys older than eight UTC days and expired delivery diagnostics after seven days; aggregate totals survive cleanup. Download visitor day is the grant's UTC day, including the possible 15-minute midnight crossing. The pure ranking contribution formula is tested, but ranking snapshots/discovery are not implemented yet.
- Concurrent-view tests exposed MariaDB repeatable-read record-change errors, first on daily keys and then on delivery completion. The affected transactions now use read-committed plus explicit project/current-row locks; no global isolation change. Targeted tests passed after correction. Master build passed with zero warnings/errors. Community regression: 115 passed, zero failed/skipped (`community-metric-regression.trx`, 105 seconds), including real database dedup/partial completion/retention, trusted proxy/bot cases, HTTP views and gateway exact-byte downloads with public/list counts. App TypeScript/lint and production build passed.
- Remaining: hourly ranking and abuse controls, broader directory filtering/search, editorial selection, SEO, browser metrics/consent verification, load/retention-backlog acceptance, and all outstanding H00–H15 integrations. Goal active; production untouched.

## Ranked discovery and editorial checkpoint — 11 September 2026

- Schema 29 and the worker now calculate atomic hourly ranking snapshots from current eligible votes and deduplicated successful download visitors over seven days. Contributions use UTC day-start age and the 48-hour half-life formula. Current membership/inactive status and moderator exclusions are checked; metadata edits do not refresh event age. Old snapshots survive for two days with bounded cleanup. Views never affect scores.
- Public directory now filters approved title/summary/tags, category, all selected tags, resource/showcase kind, exact author-listed published-release build and source availability. New/updated/trending/upvotes/downloads/editorial sorts use ID tie-breakers; exact and prefix title matches lead search results. Updated ordering excludes unapproved edits. Filter-bound cursors expire after 24 hours and pin hourly rankings across recalculation; archived/suspended creations leave trending immediately.
- Added audited revision-checked editorial picks and account ranking exclusions, including restored-eligibility revisions to prevent stale cycles. Internal reasons are administrator-only. Web moderator controls require refresh/confirmation. Public editorial shelves are labelled separately from activity ranking.
- Web directory includes URL-backed filters, removable chips, cursor recovery, ranking calculation time, native mobile filter dialog, explicit focus return and a no-JavaScript fallback. Filtered URLs are noindex. Landing shelves cover featured/trending/new/updated/showcases, with duplicate cards omitted for small catalogs and a wider editorial card. No fabricated catalog fixtures or counts.
- Targeted query tests caught integer-versus-double reader conversion; fixed with numeric conversion. A test assertion used an unavailable helper and stopped the build; the subsequent stale-binary two-test run failed its required-four minimum and is discarded. After correction, all four targeted tests passed (`community-discovery-fixed.trx`). Master build passed with zero warnings/errors. Community regression: 119 passed, zero failed/skipped (`community-discovery-regression.trx`, 109 seconds), including compatibility HTTP, private-content isolation, cursor filters/rollover/expiry, ranking math, removals/exclusions, editorial controls and stale revisions. App TypeScript/lint and production build passed.
- Still required: dynamic project/creator sitemaps and processed-image social metadata, shared links, large-corpus/performance checks, live dialog/Back/keyboard/viewport validation, remaining publishing lifecycle work, GitHub, Warden, launcher, and H15 release artifacts. Existing local Next startup rejection still limits browser evidence. Goal active; production untouched.

## Public sitemap and sharing checkpoint — 11 September 2026

- Added bounded public sitemap bucket/index APIs. Project entries use canonical public paths; creator cursors/buckets derive from public handles without exposing account IDs. Draft/suspended projects and inactive/unverified profiles are excluded; archived projects stay readable/indexable. Modification times follow approved publication/lifecycle/release events and profile updates, not private autosaves or internal curation notes.
- Added a separate dynamic Hub sitemap index and documents, advertised alongside the unchanged static documentation/site sitemap. Each shard reads at most 50 cursor pages/50,000 entries under a 20-second deadline. Invalid entries, loops, oversized shards and outages fail with retryable 503 instead of a false empty/truncated sitemap. Only occupied buckets appear in the index.
- The initial Next build pre-rendered an invalid placeholder shard response. Added request-time connection() outside try/catch and before parameter validation, following installed Next route-handler/connection documentation. Rebuild passed; both sitemap handlers are dynamic, the placeholder is absent from prerender-manifest.json, and routes-manifest.json retains the correct dynamic kind/bucket matcher. No global framework caching change.
- Project, version, discussion and creator social metadata now uses processed public cover/avatar images with actual dimensions/alt text and a brand fallback. Canonical sharing has clipboard/native-share controls and a selectable-URL fallback. CreativeWork structured data escapes script delimiters and asserts no offers/ratings. Version alias redirects retain their cursor; invalid version-page cursors have first-page recovery.
- Master build passed with zero warnings/errors; two real-MariaDB/HTTP sitemap tests passed (`community-sitemap-http.trx`, 8 seconds). Four XML/pagination/path-boundary/JSON-LD injection tests passed (`node --test scripts/test-community-seo.mjs`). App TypeScript/lint and final production build passed. Prior full community baseline remains 119 tests; no repeated full regression claimed for this isolated addition.
- Served sitemap/OG HTML, crawler fetches, clipboard/native-share/dialog focus and full browser journeys remain unverified under the existing Next startup rejection. Large-corpus performance, remaining publishing lifecycle/moderation work, GitHub, Warden, launcher and H15 release artifacts remain required. Goal active; production untouched.

## GitHub provider foundation checkpoint — 11 September 2026

- Verified current official GitHub OAuth and release-asset contracts: OAuth supports S256 PKCE; public release metadata/assets require no provider token; release assets expose SHA-256 digests; current REST request version is 2026-03-10. References: https://docs.github.com/en/apps/oauth-apps/building-oauth-apps/authorizing-oauth-apps and https://docs.github.com/en/rest/releases/assets . OAuth account connection is still outstanding.
- Added a provider-only HTTP transport with explicit API entry points, exact HTTPS destination allowlist, manual bounded redirects, no proxies/cookies/credentials/decompression, checked DNS answers and connections pinned to those addresses. Metadata/download deadlines and both declared/chunked byte limits apply through complete consumption. Upstream response bodies and signed redirect URLs are excluded from application errors.
- Added public repository/release/asset selection and download primitives. Selection verifies release membership, numeric provider identities, public/non-draft status, ZIP size/name and provider SHA-256. Fetch rechecks selected metadata and verifies actual stored bytes; changed or missing assets fail without attaching bytes to a release. Branch names are never claimed as commit SHAs. Older digestless assets receive direct-ZIP/new-asset guidance. Failed hash verification may leave an unreferenced content-addressed object for normal cleanup; shared objects are never deleted by a failed fetch.
- Master Release build passed with zero warnings/errors. Ten provider/transport tests passed (`community-github-provider.trx`, 7 seconds), covering private destinations, host confusion, redirects, absent credentials, upstream failures, compressed/oversize bodies, cancellation, partial consumption, identifier/path injection, selection, replacement, changed bytes and missing digest. These are deterministic provider fixtures, not live import acceptance.
- A read-only request to Open2077/freeroam's public releases endpoint returned no releases. No upstream release was created. Real permitted package import evidence, durable import jobs/provenance APIs, UI, OAuth, worker isolation and all remaining H00–H15 integrations are still required. Goal active; production untouched.

## Durable GitHub import checkpoint — 11 September 2026

- Schema 30 adds permanent account/request UUID import identities, pinned selection snapshots and upload associations. Import creation atomically reserves upload capacity, records redistribution consent/provenance and enqueues a job. Identical requests return the same job; conflicting selections fail. Imports share ordinary upload quotas, add 20/day, and use a private importing state that browser grants cannot replace.
- Added a separate network-enabled importer executable, with six-minute fenced leases and five-minute operation deadlines. It rechecks creator access, fetches the pinned asset, and commits exact digest/time plus quarantined artifact and normal inspection job atomically. Final authorization, expiry, stored selection and lease checks prevent stale workers or substituted inputs from committing. Missing/replaced assets terminate; transient provider failures retry the same selection. No archive/runtime execution dependencies in the importer.
- Deterministic provider plus real MariaDB/shared parser/ClamAV integration fetched a fixture ZIP, passed inspection, and confirmed that project visibility/downloads still require publication review. Initial test expected not_found where the existing delivery contract returns download_unavailable; corrected the assertion. The complete 134-test community run then had 133 pass and one stale schema-version assertion fail (`community-github-regression.trx`, 117 seconds). Updated expected schema 29 to 30, rebuilt successfully, and the migration restart test passed (`community-schema30-restart.trx`, 8 seconds). No repeated full-suite pass is claimed.
- Master Release build passed with zero warnings/errors. Source and lockfiles include the new importer project. Runtime configuration/limitations are documented in master/docs/community-services.md. A failed hash can leave an unreferenced blob; shared objects are not deleted. A crash before commit may refetch the same pin, so exactly-once upstream transfer is not claimed.
- Next required: feature-flagged provider/import APIs, web selection/status/provenance, optional OAuth, restricted deployment/container configuration and one real permitted upstream package import. Other remaining H00–H15 gates are unchanged. Goal active; production untouched.

## GitHub HTTP checkpoint — 11 September 2026

- Added default-off CommunityGitHubImportsEnabled alongside contribution controls; catalog capabilities expose effective availability. Creator-only provider browsing is rate limited and verifies current account/project write access before upstream traffic. APIs list repositories/releases/assets, preview a pinned selection, submit a request and read its private status.
- Submission re-fetches and compares the exact preview, persists only server-derived metadata and requires distribution consent. Existing request UUIDs return their authorized job even if GitHub is offline. Import status remains readable when new imports are paused.
- Master Release build passed with zero warnings/errors. One real HTTP/MariaDB test passed (`community-github-http.trx`, 10 seconds): guest/outsider denial before provider requests, forged preview rejection, missing consent, accepted submission, offline replay, private status and pause behavior. Provider responses are deterministic fixtures. Browser selection/status, public provenance, OAuth, real upstream package acceptance and remaining gates are still outstanding.

## GitHub publishing UI and provenance checkpoint — 11 September 2026

- Release editor now includes a lazy-open GitHub picker with paged releases/assets, ZIP/digest/size eligibility guidance, exact provenance preview and explicit rights confirmation. It retains the request UUID/selection for uncertain same-request retries and preserves that state across closing/reopening the details panel. Upload history recovers durable import IDs after refresh/reload and follows existing bounded status polling/manual recovery. Failed upload rows no longer prevent a fresh ZIP reservation from the release form.
- Public/private release responses expose source provenance only where the fetched import digest matches the release artifact. Published release cards show the original source, provider asset ID, fetch time and known commit. Repository-control verification remains false and separate from distribution consent. Private requests, provider credentials and inspection details do not become public provenance.
- Fixed initial TypeScript strict-index errors by handling missing split components. Final app TypeScript/lint and production build passed. Master Release build passed with zero warnings/errors. Full community regression passed: 135 tests, zero failures/skips (`community-github-provenance-regression.trx`, 154 seconds). This includes HTTP authorization/idempotency, upload-history import association, stale workers, tampered pins, real MariaDB/parser/ClamAV, review-before-publication and public provenance.
- An isolated ignored console probe invoked the actual provider transport against GitHub: repository ID 1362854332, Open2077/freeroam, zero releases. Real DNS/TLS/provider metadata works; no live upstream package import is claimed. The fixture import remains deterministic provider responses with real local downstream services.
- Remaining H10: optional OAuth connection/control verification, live browser and upstream resource-import acceptance, deployment isolation/configuration and further failure/load evidence. Warden, launcher and all other unfinished H00–H15 acceptance gates remain required. The earlier Next startup rejection remains in force. Goal active; production untouched.

## OAuth provider primitive checkpoint — 11 September 2026

- Official GitHub scope documentation confirms no-scope grants read public profile/repository information: https://docs.github.com/en/apps/oauth-apps/building-oauth-apps/scopes-for-oauth-apps . Added S256 authorization/token exchange primitives with a fixed callback path, no added scopes, public identity revalidation and optional current public repository-control checking. Tokens stay transient and are never returned to callers. Credential-bearing requests do not follow redirects; broader pre-existing grants are rejected rather than silently accepted.
- Control verification requires the authenticated personal owner or explicit push/maintain/admin permission. Organization membership or a source URL alone does not establish control. Missing permission data produces an unverified result. OAuth configuration diagnostic string redacts credentials.
- Initial test build failed due inferred tuple element names; fixed with an explicit named tuple type. Final master Release build passed with zero warnings/errors. Five deterministic OAuth provider tests passed (`community-github-oauth-provider.trx`, 6 seconds), covering the S256 vector, callback/scopes, credential destinations, redirects, transient token handling, scope/provider errors and personal/org control cases. Prior full community baseline remains 135 passed; these five additions have targeted evidence only.
- Next: implement durable single-use account-bound state with PKCE verification and connection revision fencing; connect/disconnect/callback APIs and UI; persist verification freshness and historical import attestations; configure the optional provider credentials and validate live OAuth when available. No OAuth API is exposed yet. Other H00–H15 work remains active; production untouched.

## OAuth connection and verification checkpoint — 11 September 2026

- Previous goal turn classified as progress: committed provider/import/UI changes and produced new validation. This turn adds schema 31 account-bound single-use OAuth state, S256 verifier matching, ten-minute expiry, ten starts/account/hour, revision-fenced connections and disconnect tombstones. State hashes/challenges are stored; provider tokens, authorization codes and verifiers are not. One GitHub identity maps to one current Open77 connection. Disconnect cancels in-flight state and removes future proof; old callbacks cannot restore it.
- Added connection/connect/callback/disconnect APIs behind default-off OAuth configuration. Current private status/disconnect work when new connections are disabled. Optional authorized-repository proof is fresh for 24 hours and tied to the current GitHub identity. Import creation snapshots proof time/identity; disconnect or expiry prevents future verified imports but preserves historical provenance.
- Added account connection and callback pages, profile link and repository verification controls in the release picker. Tab-local PKCE state/account/expiry is checked, authorization queries are removed before exchange, callback promises survive React effect replay without duplicate exchange, and verifiers are removed afterward. Wrong-account failures retain the callback query for the original account. Callback metadata uses no-referrer. Local disconnect explicitly distinguishes GitHub's own application revocation.
- Targeted state/revision/migration tests passed (4, community-github-oauth-state.trx); freshness/pruning tests passed (2, community-github-control-proof.trx); HTTP callback/disconnect test passed (1, community-github-oauth-http.trx). Final Release build passed with zero warnings/errors. Full community regression: 145 passed, zero failed/skipped (`community-github-oauth-regression.trx`, 136 seconds). Three browser PKCE/storage tests passed (`node --test scripts/test-community-github-pkce.mjs`), as did TypeScript/lint and production build.
- OAuth client credentials and exact callback registration have not been provisioned or tested live. No claim of real provider authorization or browser acceptance. Hosting callback-query log redaction remains part of release preparation. Existing browser-start rejection remains in force. Master ops/DEPLOYMENT.md was read through line 570 plus ops/Caddyfile while checking hosting boundaries; the deployment document's remaining lines still need reading before deployment preparation changes.
- H10 remains open for live OAuth/resource-import/browser acceptance and deployment isolation/configuration. Warden, launcher, load/restore/release artifacts and all other unfinished H00–H15 gates remain required. Goal active; production untouched.

## Warden integration preflight — 11 September 2026

- Re-read hub-base instructions, testing skill, README/server README and H11–H13 contract. Confirmed the actual frontend is WardenPage.cs embedded HTML/CSS/vanilla JS; WardenHost's Fable wording is stale. Host cookie/permission checks, 4-MiB global request limit and the loop-thread gateway must be preserved. Recorded findings in hub-base/docs/research/community-hub-warden.md and the research index.
- Found a prerequisite: master package inspection reads open77-hub.json schema/version/resourceRoots, but not the planned structured Hub dependencies/config-template mappings. These require a shared neutral contract, immutable master inspection/API metadata and matching Warden planning before installation can be complete. Runtime resource versions stay distinct from release SemVer; archive-root metadata uses a dot.
- open77_status confirmed the unrelated dev server and two live Cyberware clients on 127.0.0.1:11838, both alive, using about 11.4/12.2 GiB VRAM. No game/server was stopped or redirected. Existing sessions leave insufficient headroom for another game; proceed with isolated server/filesystem work and re-check availability for later live acceptance.
- OAuth commits: master 9aedc33, app b655e3a. Base now also contains the documentation-only Warden preflight commit; the master's shared parser submodule stays pinned to d8b863ca until actual shared code changes are reviewed. Goal active; no production publication/deployment.

## Shared Warden package metadata checkpoint — 11 September 2026

- Implemented neutral Open77.Resources.Hub schema-1 parsing and inventory binding: bounded strict JSON, exact project/version/root association, nonoverlapping portable paths, explicit project UUID or runtime dependency identities, and configuration source/destination mappings. Rejects missing template sources, unknown resources, duplicate identities, self-dependencies, manifest/preload replacements, file/directory/case collisions and overlapping destinations. No Lua execution or filesystem access in these primitives. Runtime resource-name validation is shared and now rejects trailing control characters.
- Added SemVer precedence using arbitrary-precision numeric identifiers and build-independent comparison. Bounded constraints support exact/comparator conjunctions, caret/tilde and alternatives; prereleases require explicit opt-in and cannot cross caret/tilde upper core boundaries. Legacy runtime versions remain unchanged. Concrete grammar/schema and remaining integration obligations are documented in hub-base/docs/community-hub-package-metadata.md.
- Server Release build through build-all.ps1 -AgentShell -Target server -NoDeploy -SkipTests passed with zero warnings/errors. Final focused tests: 12 passed, zero failed/skipped (hub-metadata-final.trx), covering versions, ranges, metadata and archive-root manifest handling. Broader resource check before the final large-number range-boundary fix: 386 passed, four environment-dependent database skips, one previously recorded Freeroam native-dropdown failure (hub-metadata-resources.trx, 32 seconds). The broader suite is not claimed green; focused tests were rerun after the final fix. No game/server sessions were disturbed.
- These are prerequisite primitives, not completed Warden installs. Next: connect shared metadata to immutable master inspection/persistence/public release APIs, then implement Warden dependency planning, transactions/recovery/config preservation and delegated publishing. Master submodule is still on its previous parser pin until that integration. All other unfinished H00–H15 gates remain required. Goal active; production untouched.

## Master package metadata integration checkpoint — 11 September 2026

- Previous goal turn was progress: shared contract implementation, commits and new test evidence. Master now pins base c5532358 and inspector hub:3. The worker validates package metadata with the shared parser and binds actual project/version, runtime manifests and archive inventory. Packages without a metadata file receive a minimal contract with inferred roots and no inferred dependencies/configuration. Invalid embedded identities, constraints or template mappings reject inspection.
- The immutable inspection snapshot now retains packageMetadata in the existing fenced acceptance transaction; persistence checks version/project association and inspected root order. Public/private release APIs expose the contract separately from private scanner findings. Null fields are omitted inside the contract so it can be read by the same strict parser. Existing snapshots without contract metadata return null explicitly. No schema migration or importer parser/archive dependency added.
- One initial build failed because ReleaseVersion validation returns void; corrected before running tests. Final Release build: zero warnings/errors. Eight targeted inspector/worker tests passed (community-package-metadata.trx). Full community regression: 147 passed, zero failed/skipped (community-package-metadata-regression.trx, 142 seconds). New evidence covers structured metadata through real MariaDB/ClamAV inspection, review and public reads, strict JSON round trip, GitHub import metadata, missing templates/project mismatch and older snapshots.
- Remaining metadata work includes publishing-form generation, web presentation, normalized community_dependencies queries, and Warden resolver consumption. Warden browse/plan/install/update/rollback/export, launcher, live/provider/browser gates and remaining H00–H15 release artifacts are still required. Existing Next startup rejection remains in force; no game sessions disturbed. Goal active; production untouched.

## Warden dependency resolver checkpoint — 11 September 2026

- Previous turn was progress (master immutable metadata integration and 147-test regression). Added WardenHubResolver under the planned Warden/Hub service directory. It selects exact published releases through bounded backtracking, caches complete per-project candidate lists, handles required/opt-in optional project constraints, and returns exact dependency order plus runtime start order. Limits are 100 projects, 1,000 releases/project and 5,000 search choices; cancellation and limit failures are explicit.
- Checks include prerelease opt-in, missing contract/digest, author-listed tested build or explicit per-release acknowledgement, runtime open77_version requirements, protected/platform and local/selected name collisions, project/runtime cycles and acknowledged manually satisfied external resources. External constraints use proposed replacement runtime versions, not stale currently installed versions. Runtime manifest versions remain independent from release versions. This resolver does not touch files or the game loop.
- Initial test compilation failed on target-typed constructors inside params arguments; fixed before testing. Server Release build passed through the mandated AgentShell/NoDeploy build with zero warnings/errors. Warden regression passed 173 tests, zero failed/skipped (warden-hub-resolver-regression.trx, 11 seconds). After adding the proposed-external-version correction/test, rebuilt successfully and all nine focused resolver tests passed (warden-hub-resolver-final.trx). No broad post-correction pass is claimed.
- H11 remains incomplete: catalog HTTP transport, current local/protected/build snapshot collection, installed-dependent compatibility, full file/config/load/restart plans, acceptance hashes/expiry and Warden routes/UI still need integration. H12/H13 installation/recovery/publishing and all other unfinished H00–H15 gates remain required. Master shared submodule stays pinned to c5532358; the new resolver is server-side only. No production release or game/server disruption. Goal active.

## Warden catalog transport checkpoint — 11 September 2026

- Previous turn was progress: resolver implementation and validation. Added WardenHubCatalogClient against public master release APIs. It exhausts pagination on the configured HTTPS origin (explicit loopback HTTP allowed for development), sends no account/Warden/server credentials, and disables redirects/cookies/proxy/decompression. Bounds cover pages, total bytes, release count and whole-operation time. Repeated cursors, duplicate/wrong identities, malformed contracts and unavailable/rate-limited responses fail explicitly without returning partial catalogs.
- Validates shared package metadata, exact project/version/root bindings and runtime names. Maps API resource rows back into metadata root order so alphabetic master query ordering cannot change install-root associations. Legacy missing metadata remains unavailable to the resolver.
- Server Release build passed through AgentShell/NoDeploy with zero warnings/errors. Fifteen Hub catalog/resolver tests passed (warden-hub-catalog-http.trx), including real loopback Kestrel plus production HTTP transport feeding exact release resolution. Other transport tests use deterministic handlers. No actual master/database-to-Warden integrated flow is claimed yet.
- Next: Warden browse/detail and authenticated route integration, live/build/local inventory snapshots, complete file/config/load/restart plans and acceptance fencing, followed by transaction installation/recovery and creator publishing. All remaining H00–H15 gates stay required. Goal active; no production publication/deployment or game/server disruption.

## Warden authenticated Hub routes checkpoint — 11 September 2026

- Previous turn was progress: catalog client and real loopback transport validation. Added distinct hub.view/hub.manage/hub.publish permissions to Warden's known permission set. Authenticated status and per-project release-list routes require hub.view before catalog traffic; manage/publish permissions do not implicitly grant viewing. Existing session role refresh immediately applies grants/removals. Installation/publication capabilities remain accurately unavailable.
- Normal server startup supplies the configured master origin. Invalid Hub-specific origin configuration reports catalog unavailable without breaking the server. Catalog reads execute off the game loop, with two concurrent reads, bounded deadlines and local 25-release pages; existing 4-MiB request cap is preserved. Fixed WardenHost's stale Fable description to match the embedded HTML/JavaScript frontend. Host disposal owns its created catalog client.
- Server Release build passed via mandated AgentShell/NoDeploy build with zero warnings/errors. Entire Warden regression passed 182 tests, zero failed/skipped (warden-hub-routes.trx, 13 seconds). Real Warden HTTP tests cover guest/permission denial before provider calls, live role grant/revocation, status/pagination/error behavior and concurrent saturation. These host tests use a fixture catalog; actual master/database-to-Warden acceptance remains required.
- Next required: project directory/detail browsing and Warden UI, live/build/local snapshot and installed inventory, complete plan/acceptance fencing, transaction install/update/uninstall/rollback, delegated publishing, and all other unfinished H00–H15 integrations and release artifacts. Goal active; production and unrelated game/server sessions untouched.

## Warden discovery interface checkpoint — 11 September 2026

- Previous turn was progress: authenticated Hub routes and 182-test regression. Added public master directory transport/projection with supported search/category/sorts, opaque cursor forwarding and 24-entry/8-MiB/20-second bounds. Authenticated Warden project discovery shares hub.view and the two-read limit. Invalid/private catalog states and expired cursors fail explicitly.
- Embedded Warden Hub tab now offers search/category/sort, project cards, cursor navigation and expandable release pages showing runtime versions, tested builds and contract summaries. All user text is escaped; tab/filter changes abort outstanding Hub reads. Role editor lists Hub permissions. This is initial integrated discovery, not completed install/detail/media UX.
- Server Release build passed with zero warnings/errors. All 184 Warden tests passed, zero failures/skips (warden-hub-discovery.trx, 13 seconds), including new deterministic discovery validation/filter/error cases. Node vm.Script parsed the full embedded JavaScript successfully. Actual browser rendering/navigation/keyboard/viewport checks and actual-master-to-Warden acceptance remain unproven; no screenshot or browser pass claimed.
- Remaining: complete detail/media/installation UX, installed/build/local snapshots, full plans and acceptance fences, transaction install/update/uninstall/rollback and delegated publishing, launcher and all other unfinished H00–H15 gates. Goal active; no production publication/deployment or unrelated game/server changes.

## Warden local file snapshot checkpoint — 11 September 2026

- Previous turn was progress: discovery transport/UI and regression evidence. Added read-only affected-resource tree snapshots with deterministic root-bound SHA-256 fingerprint, sorted file paths/content hashes/sizes and empty directories. Bounds cover 10,000 entries, 500 MiB and 32 levels. Portable paths, ancestor/entry reparse rejection and handle-based hard-link checks precede accepting file hashes. Entry/size/timestamp checks detect ordinary concurrent changes; this is not an atomic filesystem snapshot or a complete hostile-local-writer defense.
- Windows uses a no-follow final-component handle and handle link count. Linux uses no-follow/nonblocking open plus statx on the open descriptor, rejecting nonregular files and hard links. Runtime discovery's grouped/absolute/linked load-path support remains unchanged. Official native references and behavior/limitations are recorded in the base research document.
- Final server Release build passed with zero warnings/errors. Windows focused tests: four passed (warden-hub-file-snapshot-windows.trx). Initial symbolic-link fixture lacked Windows creation privilege; changed it to a junction without changing privileges. The same four tests passed on Linux in an isolated network-disabled/read-only runtime container, pinned digest 1fe86375600b62e6566b465da9553eef0621f13c67f40fe764cd8dbb1dee1497, 512 MiB/one CPU. Initial container failed before testing because MSTest needed writable local data; a small tmpfs fixed the harness. Linux TRX was ephemeral; terminal output retains the result. Full Windows Warden regression: 188 passed, zero failed/skipped (warden-hub-snapshot-regression.trx, 12 seconds).
- Next: connect live resource/build/local inventory and installed lockfile state to complete plans, hash/expire/accept and revalidate them, then implement staged installation/recovery/config preservation and publishing. This snapshot primitive alone does not complete installed inventory or safe mutations. All remaining H00–H15 gates stay required. Goal active; production and unrelated sessions untouched.

## Warden complete file inventories and update diffs — 11 September 2026

- Previous turn was progress: cross-platform local snapshots. Added complete published file inventory transport, bounded to 100 pages/10,000 files/500 MiB expanded/8 MiB wire/90 seconds. Counts must remain consistent and complete; identities, hashes, portable paths, ordering, duplicates/case/ancestor collisions and manifest/template presence are checked. Inventories bind back to the exact selected contract. Downloaded bytes and publication state still require revalidation before installation.
- Added pure per-resource file planning from owned baselines and current snapshots. Plans include source paths, hashes/sizes, target identity and fingerprint, with create/replace/delete/preserve/unchanged actions or blocking conflicts. Local edits/deletions, unmanaged targets and untracked filename collisions cannot silently overwrite data. Explicit configuration and untracked files survive updates; retained historical config mappings prevent later releases from silently deleting old operator configuration. New config uses its explicit template.
- Final server Release build passed with zero warnings/errors. All 196 Warden tests passed, zero failed/skipped (warden-hub-file-planning.trx, 11 seconds). New tests cover the full 10,000-file/100-page boundary, incomplete/changing inventories, malformed paths/counts/identities, missing templates, revoked selection, ordinary diffs, local drift, unmanaged files and current/retired configuration preservation. File catalog tests use deterministic responses; file-plan tests are pure. No applied-install claim.
- Next required: durable installed ownership/baselines, live/local/build snapshot orchestration, complete plan hashes/expiry/acceptance/revalidation, staged mutation journal/recovery/config/load/start handling, delegated publishing and every other unfinished H00–H15 gate. Goal active; production and unrelated sessions untouched.

## Warden installed ownership persistence — 11 September 2026

- Previous turn was progress: complete file inventories and conflict-aware diffs. Added WardenHubInventoryStore with exact installed identities/archive digests/metadata, owned baselines and historical config paths, revision fencing, exclusive mutation leases, detached validation, flushed temporary writes and installed.json replacement. State is outside watched resource roots. Reads do not create resource directories. An initialization marker makes later missing state an explicit recovery error.
- Validation rejects malformed/duplicate-key state, overlapping/outside/system/bootstrap targets, duplicate resource ownership, invalid hashes/paths, missing required dependencies, incompatible installed dependency versions and cycles. No filesystem content installation occurs here. The future journal must encompass swaps and commit inventory last; ambiguous post-rename outcomes need revision/journal recovery. Power-loss directory durability and killed-process recovery are not yet proven. Optional dependency enablement needs explicit stored choices before full orchestration; current optional constraints apply whenever the target project is installed.
- Initial Windows/Linux focused runs each exposed the same nullable-dependency JSON round-trip failure. Corrected omitted nullable constructor handling while keeping semantic validation. Final build passed with zero warnings/errors. Five inventory tests passed on both Windows and Linux; Linux used the pinned isolated runtime container and retained terminal evidence. Final full Windows Warden regression: 201 passed, zero failed/skipped (warden-hub-inventory-regression.trx, 11 seconds).
- Next: live/local/build snapshots and installed-state orchestration; complete hash/expiry/acceptance/revalidation; journaled install/update/uninstall/rollback/config/load/start; delegated publishing and all other unfinished H00–H15 gates. Goal active; production and unrelated sessions untouched.

## Warden runtime and installed-state integration — 11 September 2026

- Previous turn was progress: installed ownership persistence and cross-platform checks. Added manage-only runtime and installed-inventory routes. ServerApplication copies current selected resources/states/generations, dependencies/permissions/preloads/reload policy, active load rules/root/game build, assembly build/version and a per-process instance ID through the existing game-loop gateway. No hashing/network/file reads were added to the loop. Inventory reads remain off-loop and do not create state on unused servers.
- Program supplies the inventory store; a state/resource-root overlap disables this Hub integration instead of breaking ordinary server startup. Explicit enabled optional dependency IDs now persist with each installed package. Unselected optional dependencies no longer constrain unrelated installations; enabled ones require compatible presence and participate in cycle checks. This resolves the preceding checkpoint's optional-choice limitation.
- Build passed with zero warnings/errors. Full Windows Warden regression: 203 passed, zero failed/skipped (warden-hub-runtime-inventory.trx, 12 seconds). Six inventory tests passed in the pinned isolated Linux runtime container. HTTP tests prove manage-only access and gateway round trips with fake loop actions; actual ServerApplication/live-game orchestration is still unproven.
- Assembly build labels need release-stamping review in H15. Runtime load rules represent active options, so planning must also read/hash pending configuration rather than overwrite it. Unselected disk resources/protected-name discovery, complete plan assembly/hash/expiry/acceptance/revalidation, journaled mutations, publishing and every other unfinished H00–H15 gate remain required. Goal active; production and unrelated sessions untouched.

## Warden pending configuration snapshot — 11 September 2026

- Added off-loop, read-only configuration capture using the existing no-follow/single-link handle checks, a 4-MiB limit, bounded JSON depth and concurrent size/timestamp checks. SHA-256 covers the complete original bytes, including unrelated settings/comments; the returned projection contains only resource settings and file identity. Missing files are not created. Duplicate case-insensitive keys, invalid resource settings and unsupported schema versions fail closed. This is a resource-settings projection, not full server configuration validation or an atomic filesystem snapshot.
- JSONC comments/trailing commas, UTF-8 BOM, resource defaults and trimmed load rules match the normal loader. Initial regression found that JsonElement deserialization also needed JSONC options; corrected this and reran. Final Release build: zero warnings/errors. Full Windows Warden regression: 206 passed, zero failed/skipped (warden-hub-configuration-final.trx, 11 seconds). Three configuration tests also passed on Linux in the pinned isolated runtime container; terminal output retains that result.
- This primitive is not yet wired into plan acceptance. Disk reservations, pending-versus-active configuration checks, complete plans and acceptance/revalidation, journaled install/update/uninstall/rollback, delegated publishing, launcher and all unfinished H00–H15 acceptance gates remain required. No production publication/deployment or unrelated session changes. Goal remains active.

## Warden unloaded disk namespace inventory — 11 September 2026

- Previous turn completed configuration snapshots. Added read-only disk namespace inventory in base a173a8e2: all entries/nested resources under the configured root, including unloaded/invalid manifests, occupied non-resource paths and system/bootstrap names. Deterministic namespace fingerprint, duplicate case/normalization rejection, 100,000-entry/32-level bounds and repeat enumeration prevent partial discovery from silently authorizing a plan. Linked entries are reserved and marked unresolved without traversal. This is not a content snapshot; external runtime paths and unresolved links still require orchestration.
- Release build passed with zero warnings/errors. Final full Windows Warden regression: 210 passed, zero failed/skipped (warden-hub-disk-final.trx, 12 seconds). Four disk tests passed on Linux in the pinned isolated container. Initial tests exposed a too-specific missing-path exception assertion and Windows junction cleanup failure; corrected the fixtures and reran Windows regression. Linux evidence precedes the cleanup-only fixture correction.
- Remaining: combine disk/config/runtime/installed snapshots with dependency and file plans; hash/expire/accept/revalidate; journaled install/update/uninstall/rollback; delegated publishing, launcher and every unfinished H00–H15 gate. Goal active, no production publication/deployment or unrelated session changes.

## Warden draft plan assembly — 11 September 2026

- Previous turn completed disk reservations. Added WardenHubPlanBuilder to combine supplied runtime/configuration/disk/installed snapshots with dependency resolution and complete release file inventories. Runtime build labels override caller-supplied labels. Local names include unloaded manifests and external running resources; system/bootstrap disk names are protected. Mismatched roots, pending enabled/load changes, unresolved links, ambiguous names and occupied new targets/ancestors fail explicitly. Owned resources get current tree snapshots and file diffs. Removed resources and edited-file conflicts produce blockers; no writes or installation authorization occur.
- Release build passed with zero warnings/errors. All 213 Windows Warden tests passed, zero failed/skipped (warden-hub-plan-draft.trx, 14 seconds). New tests use deterministic catalog fixtures and actual temporary disk inventory, proving exact fresh-file plans without target creation and rejection of pending config/occupied paths. No live master, browser, Linux planner or game acceptance claimed.
- The draft is deliberately not yet exposed as an installable plan. Still required: reverse installed dependency validation; unloaded external manifest satisfaction; external protected-name policy; aggregate planning limits; removal/data-preservation plans; restart/load-rule proposals; complete immutable hash/session/expiry/acceptance and locked revalidation; transaction journal/staging/recovery; Warden publishing/launcher and all unfinished H00–H15 gates. Snapshot inputs must come from the server, never a client body. Goal active; production and unrelated sessions untouched.

## Warden installed-dependent validation — 11 September 2026

- Previous turn completed draft assembly. Base f3f0f52c adds resulting installed-project graph validation before file inventory downloads. Proposed shared-dependency updates cannot violate constraints of existing dependents outside the forward resolver closure. Missing required projects and resulting cycles fail explicitly. Existing packages retain their own optional selections; proposed replacements use the requested selections. The same validator accepts explicit removals for future uninstall planning and checks surviving dependents.
- Initial build found target-typed constructor syntax ambiguity in params-array test fixtures; made constructors explicit. Final Release build: zero warnings/errors. All 216 Windows Warden tests passed, zero failed/skipped (warden-hub-reverse-dependencies.trx, 12 seconds). Tests cover compatible/incompatible updates, missing dependencies, optional selection isolation, cycles and coordinated graph removal. These are project-graph checks, not proof of filesystem uninstall or runtime side-effect rollback.
- Still required: unloaded external manifest satisfaction and external protected-name policy; aggregate planning limits; resource-removal/data preservation and restart/load proposals; session-bound immutable plan acceptance/revalidation; transaction journal/staging/recovery; delegated publishing/launcher and every unfinished H00–H15 gate. Goal active; no production deployment/publication or unrelated session changes.

## Warden activation proposals — 11 September 2026

- Previous turn completed installed-dependent checks. Base 29d47883 adds operational proposals to drafts: transitive affected running dependents, reverse-dependency stop order, dependency-first proposed starts, exact load-path additions after current rules, and preload assets from both incoming and replaced resources. Preloads flag server/game restart and launcher preparation. Active starting/stopping transitions and activation cycles fail; stopped/missing external runtime dependencies become blockers. Explicit starts that override auto-start defaults and watcher coordination are visible warnings, together with runtime/database rollback limitations.
- Final Release build passed with zero warnings/errors. Full Windows Warden regression: 218 passed, zero failed/skipped (warden-hub-activation-plan.trx, 12 seconds). New tests verify dependent stop/start ordering, explicit selection after exclusion, removed preload restart requirements, transition rejection and unavailable external dependencies. No resources were stopped/started and no configuration was changed.
- Still required: validate exact load-rule selection behavior and watcher coordination in the real runtime; bounded/session-bound immutable plan storage and acceptance/revalidation; remove-resource/data preservation plans; journaled filesystem/config/runtime transactions and recovery; delegated publishing/launcher plus every unfinished H00–H15 gate. The activation proposal is not an executed or install-authorized plan. Goal active; no production publication/deployment or unrelated session changes.

## Warden immutable reviewed-plan storage — 11 September 2026

- Previous turn completed activation proposals. Base 85b861be adds ephemeral session-bound plan storage: detached serialized snapshots, SHA-256, ten-minute expiry, four plans per session/sixteen global, 8-MiB plan/32-MiB retained-byte caps, discard and single consumption. Stored state contains a hash of the validated session token, not the token. Consumption requires reviewed hash plus a matching freshly rebuilt draft and rejects blockers. Process restart invalidates unaccepted plans.
- Release build passed with zero warnings/errors. All 221 Windows Warden tests passed, zero failed/skipped (warden-hub-plan-store.trx, 12 seconds). Tests cover cross-session isolation, expiry, replay/hash/stale-state/conflict rejection, caller collection detachment and quota/discard. This verifies the store, not endpoint authorization or real locked revalidation. Serialization currently applies the size cap after allocation; aggregate orchestration/serialization bounds remain to finish.
- Still required: server-owned snapshot/request orchestration and choice persistence, current permission/origin checks, reviewed-plan UI/routes and mutation-lease integration; resource-removal/data preservation, journaled staging/activation/recovery, delegated publishing/launcher and every unfinished H00–H15 gate. Goal active; nothing deployed/published to production or changed in unrelated sessions.

## Warden server-owned plan HTTP integration — 11 September 2026

- Previous turn completed plan storage. Added normalized release/dependency-choice requests, stored with the draft for later exact revalidation. Planning service obtains runtime through the gateway and reads configured config/inventory/disk off-loop. Program supplies its resolved Warden config path. Added hub.manage-protected POST create, GET read and POST discard routes. POST routes require exact same Origin; unknown request fields are rejected, so the browser cannot supply local paths/snapshots. Planning shares the two-read bound and has a three-minute deadline. No install endpoint is enabled yet.
- Release build passed with zero warnings/errors. All 222 Windows Warden tests passed, zero failed/skipped (warden-hub-plan-http.trx, 16 seconds). Actual local HTTP test proves create/read/discard with stored normalized request, authorization/origin boundary, rejection of injected root, cross-session isolation and no created install targets. Catalog and game-loop actions remain fixtures; actual master/game acceptance is not claimed.
- Still required: reviewed-plan UI, live revalidation under mutation lease, aggregate limits/serialization bounds, unloaded external/protected-path policy, uninstall/data-preservation planning, staged journaled activation/recovery, publishing/launcher and every unfinished H00–H15 gate. Current plan status still accurately reports installAvailable=false. Goal active; production and unrelated sessions untouched.

## Warden reviewed-plan interface — 11 September 2026

- Previous turn completed plan HTTP integration. Added manage-visible plan creation to release rows, with explicit prerelease/optional/external/untested choices and displayed declared dependency identities/constraints. Review presents expiry/hash, blockers, exact release versions, requested permissions, target paths, file actions/hashes/sizes/reasons, load changes, stop/start ordering, preload assets and restart/rollback notes. File details render lazily in groups of 100. Operators can discard/recreate plans. Installation remains visibly unavailable until transaction integration exists; no fake install success.
- Release build passed with zero warnings/errors and all 222 Warden regression tests passed (warden-hub-plan-review.trx, zero failed/skipped, 9 seconds). Full embedded JavaScript parsed using Node vm.Script. A final text-only dependency-identity display addition was syntax-checked after that build. These checks do not prove DOM rendering, focus, layout or browser workflows. Tool rediscovery found no available browser automation capability; visual/interaction acceptance remains unproven.
- Still required: browser validation/polish including advanced dependency choice UX, active permission changes and abandoned-plan handling; bounds and live locked revalidation; full journaled install/update/uninstall/rollback; delegated publishing/launcher and every unfinished H00–H15 gate. Goal active; production and unrelated sessions untouched.

## Warden archive download verification — 11 September 2026

- Previous turn added plan review UI. Base 9c09cefd implements catalog delivery-grant POST and bounded archive streaming into caller-owned private staging. Validates fixed configured gateway origin, exact delivery path, expiry/query shape, JSON grant size (16 KiB), ZIP response encoding/type/length (100 MiB), five-minute deadline and SHA-256 against the selected release. Existing transport disables redirects/cookies/proxy/decompression and sends no account/server credentials. Error messages omit grant URLs/tokens.
- Release build passed with zero warnings/errors. All 224 Windows Warden tests passed, zero failed/skipped (warden-hub-archive-download.trx, 14 seconds). Fixture tests prove exact-byte delivery/hash checking and rejection of unconfigured hosts, wrong paths and query/fragment alterations before a second request. This does not prove actual master/files delivery or ZIP validity.
- Still required: operator gateway configuration plumbing and private empty staging-file ownership/cleanup; local ZIP/path/manifest validation; locked plan acceptance and publication rechecks; journaled activation/recovery/update/uninstall/rollback; delegated publishing/launcher and every unfinished H00–H15 gate. Partial destination bytes must be discarded on failure and never activated. Goal active; production and unrelated sessions untouched.

## Warden local ZIP staging validation — 11 September 2026

- Previous turn completed archive transport checks. Base e88a2f0c adds empty-private-directory extraction with archive SHA-256 verification, strict portable paths, duplicate/case/ancestor collision rejection, encrypted/link/special-entry rejection, 100-MiB compressed/500-MiB expanded/20,000-entry/32-level bounds, complete expected file membership/size/hash checks and runtime manifest reparsing. Parsed manifests and optional open77-hub.json must match the reviewed release; metadata bindings are checked locally. No Lua is executed.
- Release build passed with zero warnings/errors. All 227 Windows Warden tests passed, zero failed/skipped (warden-hub-archive-staging.trx, 13 seconds). Three staging tests also passed in the pinned isolated Linux container. Evidence covers valid packages, altered manifest/inventory and traversal/link/case-collision rejection. This relies on the reviewed immutable archive/master content inspection for nested preload/content policy; it does not independently repeat ClamAV or nested archive scanning.
- Still required: private staging ownership/cleanup outside watched roots, destination empty-file constraints, same-filesystem transaction layout, locked revalidation and durable journal/recovery, config/user-file merging, runtime activation, update/uninstall/rollback, delegated publishing/launcher and all unfinished H00–H15 gates. Failed extraction can leave partial private staging that must be discarded. Goal active; production and unrelated sessions untouched.

## Warden resource tree materialization — 11 September 2026

- Previous turn completed local ZIP staging validation. Added private resource-tree materialization from reviewed file actions. Incoming files come from validated extraction; preserved configuration/user files come from the original resource. Every copied source is bounded and hash-checked through the no-follow/single-link reader. Local tree fingerprints are recaptured before and after; overlap/nonempty destinations and conflicts are rejected. Removed package files are omitted, preserved empty directories survive. Original resource files are never modified by this stage.
- Release build passed with zero warnings/errors. All 229 Windows Warden tests passed, zero failed/skipped (warden-hub-resource-staging.trx). New tests prove config/user-file preservation, removal of obsolete package files from staging, unchanged original-tree fingerprint and rejection of post-review local edits before staging writes. These are temporary filesystem fixtures, not activated updates.
- Still required: staged-tree manifest validation after user-file merging; strict private-job ownership/cleanup and aggregate bounds; durable journal/config patching/same-filesystem swaps, locked runtime coordination and recovery; installed baselines and backup retention; complete update/uninstall/rollback, publishing/launcher and every unfinished H00–H15 gate. Goal active; production and unrelated sessions untouched.

## Warden merged-manifest and load-rule patch validation — 11 September 2026

- Previous turn completed resource materialization. Added public BuildValidatedAsync: reparses the merged tree, compares full manifest projections and rechecks staged content fingerprint. The unchecked materializer is now internal. Preserved user scripts that expand a wildcard cannot silently change the reviewed script/assets/preload selection.
- Added private-byte load-rule patch preparation. It verifies the reviewed SHA-256, validates complete JSONC and rejects duplicate case-insensitive keys, replaces only the load value or inserts missing load/resources properties, preserves all surrounding bytes/BOM/comments, and bounds resulting configuration size. No configuration writes occur yet; result bytes can contain secrets and must never be sent to plans/HTTP/logs.
- Final Release build passed with zero warnings/errors. All 233 Windows Warden tests passed, zero failed/skipped (warden-hub-merge-config.trx). Tests cover changed merged wildcard scripts and valid fresh staging, exact surrounding-byte preservation, missing-section insertion, stale hash, ambiguous keys and malformed JSONC. These checks do not prove journaled writes or runtime activation.
- Still required: private transaction layout/ownership, durable write-ahead journal, same-filesystem swaps and config recovery, runtime locking/watcher coordination, exact installed baselines/backup retention, real update/uninstall/rollback and publishing/launcher plus every unfinished H00–H15 gate. Goal active; production and unrelated sessions untouched.

## Warden directory swap journal and rollback — 11 September 2026

- Previous turn completed merged-manifest/config patch primitives. Added directory-transaction preparation with flushed write-ahead journal, reviewed plan hash/inventory revision, up to 100 nonoverlapping relative targets and before/after content fingerprints independent of location. Apply moves originals to retained previous trees then staged trees into place. A new recovery instance can reverse completed or half-completed swaps without deleting content. Unexpected post-activation content stops recovery; backup content is verified before moving an active target. Private job and resource roots cannot overlap; protected categories and linked ancestors are rejected.
- Release build passed with zero warnings/errors. All 236 Windows Warden tests passed, zero failed/skipped (warden-hub-directory-transaction.trx). Three transaction tests also passed in the pinned isolated Linux container. Tests use real directory renames and a manually constructed interruption between the two moves, verify repeat rollback, new-install reversal and preservation of operator edits. They do not kill a real server or prove power-loss durability.
- Still required: strict journal JSON/identity validation and corrupt/partial journal handling; owned private job creation, same-filesystem preflight/directory durability; config/inventory commit and rollback ordering, startup recovery selection, mutation lease/runtime/watch coordination, job UI and retention; full install/update/uninstall/rollback integration, publishing/launcher and every unfinished H00–H15 gate. No production or unrelated resource directories were changed. Goal active.

## Parallel integration checkpoint — 11 September 2026

- User explicitly authorized parallel agents. Work split across runtime activation/dependencies, directory transaction/recovery, and launcher/browser/host UI while root owned combined builds and installer integration.
- Launcher commit 9cfdd31 adds the host-owned Hub browser entry at configured website /resources, accessible sidebar and failure/retry behavior. Launcher suite: 442 passed, six existing opt-in tests skipped; 11 new Hub tests. Headless Edge verifies native-command payload via fixture bridge, retry and layouts. No real production destination/browser shell invocation claimed.
- Base 8b793b85 adds scripts/test-warden-hub-ui.mjs. Exact embedded Warden HTML passed Edge fixture-browser checks for keyboard navigation, options, review, progressive file display, escaping, recreate/discard/retry and view-only permission presentation. Evidence and 1440/900/390 screenshots live under ignored artifacts/warden-hub-ui. This resolves the fixture-browser gap, not actual account/master/game acceptance.
- Base 703a4b24 adds game-loop activation gates, prepared resource selection and mutation dispatch that cannot report cancellation while an already-started loop action is still executing. Runtime dependency declarations now share actual host parsing, including bounds and reverse running dependents. Synchronous Lua preparation/package publication still needs performance and live validation.
- Base 7967d8cd hardens directory WAL schema/identity/JSON and preflights every target before apply/rollback. Base b495e8d2 connects reviewed-plan revalidation, verified ZIP downloads, merged staging, directory swaps, private config patching, runtime activation and inventory commit. Failed recovery retains the runtime gate and blocks another installation. Gateway origin configuration is explicit; installation is not enabled in HTTP yet.
- Combined Release build passed with zero warnings/errors. Full Warden suite: 246 passed, zero failed/skipped (warden-hub-integrated-install.trx). Earlier runs passed all tests but exited nonzero solely because minimum expected counts were set one too high; corrected final run passes. Subsequent recovery-gate change rebuilt clean and focused installer scenario passed (warden-hub-install-recovery-fence.trx). Real ZIP/disk/config/inventory scenario installs 1.0, updates to 1.1 preserving operator files, restores 1.1 after injected activation failure, and fences mutations after injected runtime restoration failure. Catalog transport and runtime are fixtures.
- In progress: startup crash reconciliation, asynchronous host install jobs/review UI, runtime publication audit. Still required: actual master/files/runtime/game integration, restart/preload flow, explicit committed rollback/uninstall, Warden creator publishing, private job lifecycle/durability, and all unfinished H00–H15 acceptance/operations gates. Goal remains active; no production publication/deployment or unrelated sessions changed.

## Warden startup recovery and install jobs — 11 September 2026

- Previous checkpoint made concrete progress: committed integrated installer, launcher/browser evidence and dependency fixes. Added startup reconciliation before preload discovery/Lua construction, followed by configuration reparse. Incomplete metadata is prepared outside authoritative jobs and registered by rename only after complete artifacts exist. Installer records exact inventory-after before directory WAL so recovery can identify the full commit point.
- Recovery validates job names, bounded strict artifacts, plan/WAL/config identities and complete inventory. Uncommitted swaps/config roll back; a lost final phase after inventory commit preserves committed files. Operator edits reject recovery. Unresolved runtime activation/restoration retains the resource-control/watcher gate, including failed Lua startup slots.
- Warden now has reviewed-hash/session/current-permission/origin-protected asynchronous install receipts, status and cancellation; accepted jobs survive request disconnect and cancellation remains pending until rollback completes. Startup reconciliation enables the configured installer. Browser fixture verifies review consent, install receipt/poll/cancel. In-memory receipts are not yet durable history; preload restart path still rejects installation explicitly.
- Combined Release build passed (zero warnings/errors). Warden/runtime suite ran 256 tests: 255 passed; one root installer initialization-path bug was found and corrected (checking a not-yet-created final directory instead of its existing parent). After correction, all five installer/startup-recovery tests passed on Windows and pinned isolated Linux .NET 10.0.12. Reports: warden-hub-jobs-startup-runtime.trx (initial diagnostic), warden-hub-startup-recovery.trx (corrected Windows); Linux counts in tool output. Other 251 tests were unchanged by that parent-path fix. No actual process-kill/power-loss, live master delivery or game-client acceptance claimed.
- Parallel next work: native filesystem preflight/directory durability; publication failure retry/off-loop design; creator publishing contract integration. Still required: durable operator history/retention and explicit rollback/uninstall, restart/preload workflow, real service/game checks and remaining H00–H15 gates. No production publication/deployment; goal active.

## Native filesystem and publication retry checkpoint — 11 September 2026

- Previous turn was progress: startup recovery and asynchronous jobs were committed and validated. Base b80e0092 records host/UI integration; 5721a823 adds native opened-directory filesystem identity preflight (Windows volume GUID, Linux mount ID including separate mounts of one device), preflight across every swap, and Linux directory fsync. Base 222a8251 retries pending publication notifications without advancing an unchanged catalog generation and delivers to later observers after one fails.
- Clean Release build and full Warden/runtime suite: 261 passed, zero failed/skipped (warden-hub-filesystem-publication.trx). Pinned isolated Linux: 16 filesystem/directory/recovery/installer tests passed, including distinct-device and same-device distinct-mount rejection. Windows-only and Linux-only branches exist inside filesystem test methods; overall Windows count does not imply Linux branch execution.
- Base c0596127 extends Linux directory flushing to authoritative job registration, phase/config replacement and inventory commit, and documents trusted gateway configuration, permissions, update/cancellation/recovery behavior and current limits. Added schema/editor completion and commented example gateway origin. Subsequent Release build clean; 15 focused inventory/recovery/installer/export-preview tests passed on Windows and Linux (warden-hub-export-durability.trx for Windows). Export preview is still uncommitted and its final path-hardening edits need the next coordinated build before a final acceptance claim.
- Master/files HTTP services currently not listening at 18090/18091; MariaDB13316 and ClamAV13310 remain available. Open77 status shows two unrelated alive Cyberware clients on11838 and roughly11GiB VRAM in use; preserved both. No real-game acceptance or new game launch claimed.
- Parallel active work: move Hub script/digest/package preparation offloop; master schema32 creator device/delegation lifecycle; Warden export preview/publishing integration. Still required: actual full local service delivery/game scenarios, committed rollback/uninstall and restart/preload workflow, retained-job lifecycle, creator publishing completion and remaining H00–H15 acceptance/operations work. Windows directory flushing and process/power-loss durability remain unproved. Goal active; no production publication/deployment.

## Live private upload and off-loop/export checkpoint — 11 September 2026

- Previous turn delivered verified filesystem/publication changes. Started owned loopback master18090, files18091 and worker against dedicated local MariaDB13316/Clam13310, then copied their artifacts to ignored artifacts/hub-live-bin and restarted there to avoid build-output locks. Current tool sessions: master91838, files69276, worker28565; old direct-build hosts were verified/stopped. Master catalog/files health returned200. Snapshot currently predates schema32 delegation work.
- Added scripts/community-live-smoke.ps1 in master: fixed loopback targets, actual account registration/verification/login, project/release draft, streamed private ZIP upload, completion and worker polling. Real worker accepted the554-byte Lua package with no inspection error. Sanitized evidence: hub-master/artifacts/hub-live-smoke/a1380c808e0244a39f0eb7b9e368f955.json; project5dd8e29f-3490-4cda-8f45-91643d0d18fb, release7e6da80e-b94f-4c19-bce8-3bdeeaad9711. Tokens/passwords are absent from evidence. Original poll terminal list omitted accepted and waited the full three-minute bound; corrected list now stops at accepted and asserts success. This proves private upload/inspection across real HTTP services, not public review/download/Warden installation or game behavior.
- Base c2d9b65f moves Hub script/digest/signed-catalog preparation into the worker-side adapter and commits prepared inputs on the loop. Tests prove activation/rollback after source files disappear and rejection of changed reviewed hashes. Full combined Warden/runtime run:268 passed, one export staging-path failure (warden-hub-offloop-export.trx). Export path fixed; clean rebuild and six focused preview/ZIP/session tests passed Windows and Linux (warden-hub-export-zip.trx). Actual ZIP generation revalidates selection and metadata, bounds bytes and retains session/single-use review. Heuristic secret scanning is not proof that all possible secrets are absent.
- Master schema32/device connection tests in progress; isolated MariaDB lifecycle tests already pass, full regression being corrected for expected schema assertion. Runtime followup must retain original active inputs/catalog rather than reading pending unrelated edits. Warden export HTTP/UI/delegated publishing and preload archive inspection remain incomplete. Live public review/download/install/game acceptance, explicit committed rollback/uninstall/restart workflow and remaining H00–H15 gates stay open. No production publication/deployment; unrelated games preserved; goal active.

## Real release review/download and Warden installation — 11 September 2026

- Extended fixed-loopback community-live-smoke.ps1 with optional ReviewAndDownload. Creates a unique local reviewer, promotes only that exact account/email against verified127.0.0.1:13316 test DB, reviews release/project through real HTTP APIs, obtains a guest delivery and compares returned ZIP SHA-256. Initial PowerShell MySqlConnector logging-assembly load failed; resolved installed ASP.NET shared framework dependency. Successful sanitized evidence: hub-master/artifacts/hub-live-smoke/0d4623e25ab14975addf9ca0ac1e25b6.json, project9c2046e4-c212-4ad6-afcb-629231818773, release533572d2-385f-4a4b-90d4-7703f87be7bb, downloadVerified=true. This publication is only in the isolated local Hub DB, never production.
- Started separate copied-binary dev-local server session60099 at UDP11888, Warden11889, resource downloads11890, empty owned resources under hub-base/artifacts/hub-live-server. Existing unrelated games/servers untouched. New test-warden-hub-live.ps1 privately obtains setup/login, creates server-owned review plan with explicit untested-release acknowledgement, accepts plan, polls job and verifies installed runtime. Real master/filegateway/download/staging/runtime/inventory chain committed revision1 and hub_live_sample1.0.0 is running. Evidence artifacts/hub-live-server/install-evidence.json; job65f7393d-bfee-4441-a01b-dcbb85282f57. Local smoke-credentials.json and server setup log are private, excluded from evidence. No game-client behavior tested.
- Subsequent combined Release build clean; all271 Warden/runtime tests passed, zero failures/skips (warden-hub-active-inputs-export-http.trx). Includes retaining prior active inputs/published packages across unrelated disk edits, prepared-script aggregate limit, and local export HTTP/archive authorization. Browser fixture also passes explicit export selection/review/consent/download/discard. Live server uses the preceding copied artifact snapshot; newer regression evidence is separate.
- Master schema32 full152 community tests passed; final self-disconnect addition rebuilt and five focused tests passed. Parallel next: transactional delegated draft/upload authorization, Warden connection private-storage/UI, web consent, sharedResourceComposeCatalog submodule pin after commit. Still open: real update/rollback/uninstall/restart-preload/game acceptance, creator upload completion, durable history/retention and remaining H00–H15 gates. Goal active; no production deployment/publication.

## Website Warden consent and revocation — 11 September 2026

- Previous turn proved real local service-to-Warden installation. Added /account/connections and /account/connections/approve with login/verified-email gates, manual human-code preview, device label/scopes/expiry, explicit unchecked consent, selection of one new project or an existing project, paginated project choices, approval success return-to-Warden guidance and connection listing/revocation. New-connections-paused state still permits revocation. Hub navigation links to the connection manager. Browser only uses the existing account session; device/access/refresh credentials for Warden never enter these components.
- Read installed Next use-client documentation before implementation. npm run check passed (TypeScript and ESLint), npm run build passed with363 generated routes including both new pages. A final text-only clarification names the one-new-project scope explicitly. Browser interaction/rendered consent acceptance remains unverified; the earlier automatic Next startup rejection is still respected, no alternative Next launcher used.
- Parallel active work: master schema33 transactional scoped export/upload integration, Warden persistent operator-bound refresh credentials plus connection UI, and maintenance plans/removal support for explicit committed rollback/uninstall. Tested shared base b2f983dd is available for master pin reconciliation. No production deployment/publication or unrelated-session changes; goal active and remaining H00–H15 gates unchanged.

## Hub container preparation — 11 September 2026

- Previous turn added checked/built web consent. Read the remaining deployment runbook through line776 before preparing operations artifacts. Added .dockerignore excluding local configs/keys/runtime/test/build output; ops/community/Dockerfile with digest-pinned SDK10.0.302 and runtime, locked restore, nonroot files/worker/importer targets; opt-in compose overlay with disabled Hub flags, separate service database variables, private/scratch mounts, memory/CPU/PID limits, dropped service capabilities, scanner digest and no host ports or CDN/key mounts. Added rollout/rollback ordering and explicit remaining restore/least-privilege gates.
- Docker Compose config validation passed. All three Linux images built successfully from current master/shared-base source. IDs: files sha256:d3aa271aa3da75dbbc97bbd98833fadb8cce58acd19be3b1767916e9012adb1f; worker sha256:e4a9a072c16ea22d2b8737baa94950490db3f9669676840c799d93b58cc75931; importer sha256:51a432470b4a674cd5354e4905e05cdd793ecbe8ac028565a2e180836cd7596a. All report/runtime-check UID1654; isolated no-network read-only inspections confirm no /app/master.json, /app/data or /source. These are local development images, not immutable release candidates; schema33 source was still under active implementation/testing. No containers from the deployment overlay were started.
- Native creator lifecycle code/browserfixture ready; combined base build waits for the maintenance planner to reach its announced coherent checkpoint. Master scoped export regression still runs independently. Remaining H15 restore/offsite/grant/edge/resource-cap/CI validation and all unfinished feature gates stay open. Existing copied local HTTP/runtime services and unrelated game sessions preserved. No production deployment/publication; goal active.

## Maintenance execution, creator lifecycle and service grants — 11 September 2026

- Root coordinator/recovery commit 8a47126c and runtime planner/primitives e82653b1 implement separately reviewed uninstall/committed rollback, immutable session-bound plans, server-owned revalidation, private staging, preserved configuration/user files, explicit directory removal, new durable job history and pre/post-inventory-commit startup recovery. Historical backup jobs remain read-only. Shared configuration/job file helpers are used by install and maintenance. Restart/preload operations still require their unfinished restart flow and are rejected by this live executor.
- Combined Release build initially found five creator route delegate analyzer errors; corrected. First290-test run had276 passes and14 invalid scriptless maintenance fixtures; fixed fixtures, then corrected one missing private staging directory in a test. Final clean Release build and all290 Warden/runtime tests passed, zero failures/skips (warden-hub-maintenance-final.trx). Pinned Linux runtime ran17 maintenance/creator tests successfully, zero skips. Actual installer update followed by committed rollback is exercised with configuration edited before and after update; the latest value and unrelated user files survive. These are integration tests against filesystem/runtime fixtures, not live game-client acceptance.
- Creator lifecycle committed d565e8f6 with explicit route results, private refresh-only persistence, current operator/master binding and human-code grant validation. Its native tests passed in the combined suite and its earlier embedded browser fixture passed. Scoped upload/publishing implementation is now in progress against master schema33; native lifecycle success does not prove that complete flow.
- Master af9ec9b scoped export APIs/schema33 passed157 community tests with real MariaDB/ClamAV, followed by eight focused late-worker tests. Retention5259b5c passed11 focused tests and preserves approval/export audit identity while pruning expired devices and scrubbing old credential digests. Grantsb0ac903 adds separate files/worker/importer column/table templates and a runnable isolated-principal validator: intended upload/import/scan/media/delivery/notification/maintenance branches pass;29 forbidden operations are denied. Current community test inventory161 is not a claim of a full161-test run. SQL grants cannot constrain state-transition values or shared-queue row kinds; stronger compromised-service isolation remains an architectural gap documented in ops/community.
- Open77 read-only status confirmed two unrelated alive Cyberware clients on11838, PIDs32104/57232, using11167/12227MiB VRAM; preserved them. Existing copied Hub local services remain the previous snapshots and must be refreshed before current-version live acceptance. No production publication/deployment or unrelated worktree changes.
- Next: maintenance HTTP/jobs/history wiring, Warden publishing to real scoped endpoints, maintenance UI and actual update/rollback/uninstall/restart-preload/game checks. Retention/history policy, GitHub real import/OAuth, performance/load, restore/offsite/edge/resource-cap/CI and remaining H00–H15 acceptance gates stay open. Goal active; this checkpoint is not release readiness.

## Real creator publishing and live maintenance — 11 September 2026

- Previous goal turn was progress: maintenance execution/recovery and restricted grants were implemented and tested. Refreshed only three verified owned local Hub hosts into artifacts/hub-live-schema33-b0ac903; schema33 master now session90470 at18090, files5728 at18091, worker51595. Existing local DB, storage and identity preserved. Re-ran real upload/inspection/review/download successfully: sanitized run dbc2992b5ff241cfa419dde85d0df559, project9b7a8253-a7af-4a13-884a-c326dfcd8492, releasea92e6b30-a392-4c82-bb02-cb4ed55d435a,555-byte SHA3777bbbecfac856ea1b7ab0d1a71a4667e840a860a282213463d6045dcf5fa96.
- Updated only the owned local test game server, retaining previous binary snapshots and private config/resources. Current copied snapshot bin-creator-acl-final runs in session41284 at Warden11889/UDP11888. Real creator poll initially failed private directory protection even though native fixture tests passed. Safe diagnostics isolated Windows SetAccessControl; removing unnecessary SetOwner while retaining a protected DACL limited to current process user and SYSTEM resolved the live failure. Canonical directory paths alone did not fix it. No ownership privilege diagnosis is claimed beyond this observed fix.
- Base021c467b includes scoped publishing, joint maintenance UI, ACL fix and tests. First297-test run had296 passes and one malformed-history HTTP500 instead of409; InvalidDataException handling corrected. Final clean Release build and all297 Warden/runtime tests passed, zero skips (warden-hub-publish-maintenance-http-final.trx). Embedded Node/Edge fixture passed publishing and maintenance selection/history/review105-files/escaping/keyboard/hash-consent/cancel/mobile flows. Working source now includes subsequent unbuilt durable publishing and shared inspection work; these297-test claims apply to the copied tested checkpoint.
- Root6175d741 adds reproducible fixed-loopback creator and maintenance smoke scripts. test-warden-hub-creator-live.ps1 -PublishSample proved Warden human-code approval, master account verification, token polling, private refresh persistence/rotation, server-owned reviewed639-byte ZIP, scoped new-project upload, real file gateway/Clam worker, accepted private draft verified through creator project listing, and disconnect/master revocation. Evidence creator-evidence.json: projectb71c3ee5-b3de-4563-9e78-0aefc5e483f7, exportd4646577-43a3-432c-85fe-66ef160b79f0, SHA17eae3000ba6166b70efa193c050efc3bbc2925cc522e2e2fe86ab8676643383. Credentials and human/device/access codes are absent from evidence.
- test-warden-hub-maintenance-live.ps1 proved real reviewed uninstall of owned hub_live_sample project9c2046e4-c212-4ad6-afcb-629231818773 (inventory1→2; no running resource), then exact release533572d2 reinstallation (inventory2→3; sample running). Durable jobsd106386f-54ed-4248-9fb0-f6bbc409a413 and42c7e56c-4e10-42fb-b30a-e1cebff0c3ec; evidence maintenance-evidence.json. No game-client/restart-preload acceptance claimed. Unrelated clients/servers remained untouched.
- Masterffe5e57 adds reproducible isolated real mariadb-dump/SQL+private-tar restore drill and sanitized ops/community/evidence/restore-2026-09-11.json. Exact metadata, package/avatar HTTP SHA checks, corruption rejection and single pending-image recovery passed using restricted restored principals. Current community test inventory162 is not a full162-test claim. Production signing continuity, Linux ownership/ACL restore and off-site retention remain unproved.
- Next: durable publishing receipts/resume/stream digest; authoritative shared nested preload inspection and Warden integration; history/backup retention; remaining live update/rollback/restart-preload/game scenarios, GitHub OAuth/import, performance and full H15 operational/CI gates. Maintenance HTTP/history final commit is coordinated separately; no production changes. Goal remains active and incomplete.

## Shared inspection, restart-resume and retention checkpoint — 11 September 2026

- Base f21f0bc8 moves ZIP staging, content policy, cumulative budgets and nested preload inspection into the shared Resources assembly. Warden and master consume it; master a7f7983 pins that revision and adds export digest/length responses. Full master community regression passed162 tests; final provenance-label adjustment rebuilt and passed11 focused tests. Provenance: f21f0bc8;hub:4.
- Master0217f69 adds locked/digest-pinned root image builds, four-image validation and Ubuntu CI. All four development images built and passed nonroot UID1654, no-network/read-only config/source/key exclusion checks; Compose and CI YAML validation passed. artifacts/hub-images/validation.json records source state/image IDs. These dirty development snapshots are not final release candidates; no remote CI run or deployment-overlay startup claimed.
- Refreshed only owned master/files/worker to copied a7f7983 artifacts (sessions9215/10772/68071; ports18090/18091 and existing dedicated DB/scanner). Copied server bin-resume-preload runs session53376 on Warden11889/UDP11888. Configuration, identities, resources and unrelated sessions preserved.
- Root cbbf7247 adds server instance evidence, test-warden-hub-resume-live.ps1 and retention access at the1000-job creation limit. Real private publishing created project43c88e45-9c4a-45e2-9abb-2b5999500df1, export001363fe-394f-49f1-8ea1-7d37ee528f92, job78c59f2b-fd6b-4f80-93c3-c66477d5ea12;639-byte ZIP SHA17eae3000ba6166b70efa193c050efc3bbc2925cc522e2e2fe86ab8676643383. After an actual owned-server restart, a new session resumed the same accepted draft/export/hash and disconnected. Instance2342ea6c-0a43-4bc7-b48a-9dd5ab10d5ec changed to c2a5670a-4bd7-4a50-8b26-3a11608df3f5. Sanitized creator-evidence.json/resume-evidence.json contain no credentials. This proves accepted-draft restart recovery, not interrupted real PUT/create response recovery.
- Base a6292fea commits token-free fixed-request receipts, explicit current-session resume, stream/master-artifact digest fences, shared preload export inspection and conservative private spool cleanup. Retention uses reviewed hash/lease-protected expiry, minimum30days/newest3 committed installs, current/source-job protection, exact payload verification, unique config/user/modified backup preservation and resumable detached cleanup. Joint embedded browser fixture passed retention/publishing; standalone retention commit coordinated separately.
- Clean combined Release build: zero warnings/errors. All316 Windows Warden/runtime tests passed without failures/skips (warden-hub-retention-spool-final.trx); pinned isolated Linux27 focused shared-inspection/publishing/export/retention tests passed without skips. Earlier310-test checkpoint is superseded. Prior Linux output lost at compaction is not counted; the27-test run is observed evidence.
- Active parallel work: real-service lost-response fixture, restart-required install/maintenance and isolated10,000-project performance measurement. Actual app browser/GitHub provider/game validation and remaining operational/release-candidate gates remain open. No production publication/deployment; goal active and incomplete.

## Real update/rollback and interrupted publishing — 11 September 2026

- Previous goal turn made progress: shared inspector, durable accepted-draft resume and retention/spool validation were recorded; retention standalone commit9e391a4c completed the316-test snapshot. Root added master9dd8dfc paired-version local publication fixture and base3170b29a real update/rollback script. Both releases pass real upload/worker inspection/review and exact downloaded SHA verification before Warden use.
- Initial live update succeeded, but the smoke passed its HTTP tracking jobId to rollback instead of installationJobId. This produced409 local_snapshot_unavailable; no planner defect established. Corrected script uses durable installationJobId. New fixture declares the observed exact tested server build and a unique resource name; the earlier owned fixture remains untouched for diagnosis.
- Full corrected run: project56acfa6f-088f-4337-90c3-8653e67db8bd, resource hub_live_update_1804065c; installed release0de0781d-3b64-4407-b53a-95a2da15dff4 version1.0.0 at inventory6, updated release684fa33d-908b-4d2d-b71c-9626ce4f8127 version2.0.0 at7, rolled back to running1.0.0 at8. Config edited before update survived; a later config edit plus untracked user data survived committed rollback. Durable jobs6bebc32a-7fff-4748-a67f-348ed7f1b602 /2bdce485-b0dd-48e7-92ca-4f613d5b00e3 /007e3243-b9e5-4fff-9ca1-73375457cbd1. Sanitized artifacts/hub-live-server/update-rollback-evidence.json; master artifacts/hub-live-smoke/1804065c482a4029966d2682fa666654-maintenance.json. Copied bin-resume-preload server remains running; no game-client acceptance claimed.
- Agent basebc0c6d4a adds real loopback lost-response fixture and docs. Separately owned Warden/proxies prove master-create response loss followed by process restart/idempotent replay (two identical create requests, one project/export/upload), and gateway PUT response loss followed by restart/digest recovery (one create/one upload). Both become accepted private drafts. Sanitized artifacts/hub-fault-e4ab7192-4cff-4394-afcf-9474b6f01ef6/fault-evidence.json. Own hosts stopped and connections revoked; shared services/11889 untouched. Initial fixture lacked installation metadata and was corrected before successful full run.
- Remaining active work: restart/preload transaction, performance harness and complete website user-flow audit. Browser/provider/game and remaining operational/release-candidate gates remain incomplete. No production deployment/publication or unrelated changes; goal active.

## Linux private-storage restore — 11 September 2026

- Previous turn made progress with real update/rollback and lost-response recovery. Master74cc9a7 adds ops/community/test-linux-storage-restore.ps1, documentation and image-CI integration. Three disposable Linux Docker volumes exercise numeric-owner tar backup/restore with service UID/GID1654, directories0700/files0600, exact restored file SHA, post-restore service write and a different UID denied read/write. Pinned runtime image1fe86375600b62e6566b465da9553eef0621f13c67f40fe764cd8dbb1dee1497; no operator storage/keys/network mounted.
- Actual local drill passed, sanitized artifacts/hub-linux-restore/evidence.json. Fixture SHA f6dfcf792dc362bd027e5daaeff2b22b3abe18d69317d654e306008d602eb3c8. All three owned volumes removed, verified by Docker listing. Initial PowerShell array concatenation produced an invalid volume name before creating any volume; corrected and reran successfully. CI YAML parsed and expected step verified; remote CI not run.
- This establishes Linux modes/UID/GID on Docker named volumes, not release-host ACL/xattrs, encrypted/off-site backups or production signing continuity. Those gates remain. Performance harness is running an isolated real10,000-project load experiment; no final result yet. Restart flow and website fixes remain active. Goal incomplete; no production changes.

## File edge and restart core checkpoint — 11 September 2026

- Previous turn made progress with the Linux restore drill. Masterc50f948 adds standalone ops/community/Caddyfile, reproducible test-edge.ps1 and integration documentation. It preserves original paths, allows upload/delivery/media methods with CORS preflight, denies other routes/private storage/health, sets100-MiB upload and1-KiB read-body limits, and redacts URI/request/response headers from access and runtime logs. Existing master/CDN config is untouched. Official Caddy body-limit/log-filter references are linked in the operations guide.
- Pinned Caddy image13ba145cba2f3e28fa801994876e4c086d1b95d5aa2a520a734765ffb6b12017 validated config. Two owned ephemeral loopback edges proved real gateway401 for invalid grants,404 for unsupported/private paths,502 on unavailable upstream, response security headers and absence of synthetic credentials/query values in access/error logs. Initial container invocation/capability errors were corrected; Caddy executable requires NET_BIND_SERVICE. First unavailable-upstream check found missing security headers; added error-handler headers and reran both scenarios successfully. artifacts/hub-edge/evidence.json records the result; owned containers removed. Full-size streaming boundary, production TLS/DNS/CDN cache/redaction and outer rate limits remain unverified.
- Restart core source checkpoint builds with zero warnings/errors; all320 Windows Warden/runtime tests passed, zero skipped (warden-hub-restart-core.trx). Pinned Linux InstallCoordinator class passed6 tests, including staged preload startup/commit, revocation, failure rollback and cancellation. Initial filter used the filename rather than its partial class and correctly failed minimum-test policy with zero tests; corrected run is the evidence. Core includes pending private staging, startup authority/content revalidation before preload resolution and Lua/inventory gate; UI/additional crash tests and actual separate-process smoke remain active, so H12 is incomplete.
- Performance experiment exposed ClamAV timeout for a100-MiB single-line fixture; agent retained fail-closed policy and changed to deterministic asset-like binary bytes. Subsequent run exposed slow public catalog queries; query-plan investigation continues. Root server tests overlapped that shared-host run and this concurrency caveat was reported to the agent; no passing performance claim. Website usability fixes continue. No production or unrelated changes; goal active.

## Full-size edge streaming boundary — 11 September 2026

- Previous goal turn made progress with route/redaction checks and restart core validation. Master1516243 adds standalone dependency-free ops/community/edge-fixture and locked restore/build instructions. It starts an owned loopback Kestrel consuming backend with its request-size cap disabled and an ephemeral Caddy using the exact prepared config. This ensures the edge, rather than a backend cap, enforces the boundary.
- Clean Release build and locked restore passed. Actual streaming cases104857600 bytes with Content-Length and chunked framing both returned200 with the exact backend byte count;104857601 bytes with each framing returned413. Four requests completed in roughly0.9–1.0seconds each; this is local fixture timing, not representative network throughput. Generator buffer64KiB; no real accounts/grants/database/storage used. artifacts/hub-edge/boundary-evidence.json records pinned image and results. Owned container absent after cleanup and backend disposed.
- Agent load run reported zero HTTP errors, account p95 regression+3.94% and server-directory−1.88%, but public catalog p95 failed and scanner acceptance exceeded the180-second bound. Query plans show full revision JSON/projections materialized before page limiting; SQL optimization is under validation. These mixed results do not close H09/H15. Website fixes passed check/build and await final commit; actual browser journey remains open. Restart additional tests/live smoke continue. No production changes; goal active.

## Operator configuration, guides and restart follow-up — 11 September 2026

- Previous turn made progress with full edge streaming boundary. Root masterb9f7a45 fills missing Hub configuration in .env.example, master.example.json, Compose and generated final-provider master.json. Eight strict true/false flags, OAuth settings, gateway and trusted-proxy array render explicitly; invalid flags/control characters reject without echoing values. test-entrypoint.ps1 exercises defaults/enabled flags, string escaping and rejection in network-isolated containers with synthetic values and a dotnet shim; no master/database/key startup. All checks passed; initial shim mount was non-executable, corrected fixture tmpfs exec and reran. Compose config, example JSON and CI YAML validate. CI wired, remote run not claimed. Operator-adjustable quota/job ceilings remain a separate configuration gap.
- App521a1da fixes Warden release deep link, bounded draft-load retry, media status/selection, retained reply drafts, moderation retry/focus and screenshot lightbox. npm check,15 existing tests and Markdown safety passed; production build363 routes passed before final one-line draft timeout, then check passed again. Actual rendered app/browser acceptance remains unverified and the earlier Next startup rejection remains respected.
- Appb419fc5 adds authored public Hub guide/navigation and operator handoff, corrects Warden restart wording; basef4572f34 corrects metadata guide's obsolete pending-integration claim. Documentation coverage0 problems (11 authored guides/61 nav pages), no build during load measurement. Release-pinned links/base wiki sync and final working-flow updates remain.
- Restart follow-up source built cleanly; all326 Windows Warden/runtime tests passed, zero failed/skipped (warden-hub-restart-followup.trx); isolated pinned Linux InstallCoordinator11 tests passed without skips. Coverage extends staging tamper, pending operator edits, preload maintenance removal, interruption/cancellation and cross-session management. Agent preparing separate copied12088/12089/12090 real-service restart smoke; existing11889 server untouched. Implementation commit coordinated separately.
- Optimized public directory query now limits narrow candidate rows before projecting full revision JSON. Agent measured all8 public routes p95<500ms, two exact100-MiB zero-filled fixtures accepted, but account regression38.59% failed while services shared the same affinity cores. Next run uses disjoint CPU sets with unchanged capacities and reports that topology; no H09 acceptance claim yet. Arbitrary payload scanner timeouts remain documented. No production publication/deployment; goal active.

## Measured catalog performance and operational follow-up — 11 September 2026

- Previous turn made progress with config rendering, guides and restart tests. Master6633e96 commits narrow-page directory SQL, reproducible benchmark and sanitized reports. Full163 Community tests passed. Controlled final run: all8 public loaded p95 values59.30–464.29ms, aggregate359.11ms; account69.67→48.12ms (−30.93%), directory55.38→41.72ms (−24.66%).2675 public/428 account/428 directory loaded requests, zeroerrors; two exact100-MiB uploads accepted92s/107s including74s paced transfer. Filespeak78,336,000B, worker100,040,704B, scratch104,857,288B. See ops/community/BENCHMARK.md and evidence/benchmark-2026-09-11.json/history. Limits: native Windows/disjoint2+1+2 logical CPU affinity, observed rather than enforced memory/scratch caps, zero-filled asset payload, no published-release history in10k catalog,7 captured queryplans/8 routes. Earlier arbitrary large ASCII/binary scanner timeouts retained as limitations; no universal throughput claim.
- Base2ef076f0 commits restart implementation. Additional real-Lua preload update→restart rollback fixture built cleanly and all12 InstallCoordinator tests passed Windows and pinnedLinux (warden-hub-restart-rollback.trx). Previous full326 suite remains the full-suite evidence;327-test full run is not claimed. Extra test/prepared scripts commit coordinated separately.
- Automatic command review rejected the isolated12088/12089/12090 server launch as blocked by policy without a narrower reason. Preparation/read-only checks succeeded; no launch retry. Concrete start-warden-hub-restart-fixture.ps1 now verifies artifact hashes, fixed loopback config and free TCP/UDP ports, starts only its copied owned server and keeps setup credentials private. A user approval request for that exact prepared launch is pending. No game/isolated process restart acceptance claimed.
- Root added admin-only bounded queue/reservation/moderation/outbox aggregate code and test, awaiting coordinated master build; no readiness/telemetry completion claim yet. Launcher agent implementing shared tightening-only policy and schema34 per-upload inspection snapshot; transaction agent investigating enforceable auxiliary service state authority. Runtime agent preparing canonical guide sync. These are active implementation items, not completed gates. No production changes; goal active.

## Operational monitoring and policy integration checkpoint — 11 September 2026

- Previous turn made progress with measured performance, prepared restart approval and administrative aggregates. Agent combined master build passed;169-test Community run finished168passed/1failed/0skipped. The failure was migration34 replay adding an existing inspection-policy column; corrected to IF NOT EXISTS, awaiting final combined validation. New quota/snapshot tests and root operations aggregate test passed in that run. No claim of a passing full169 suite.
- Root implemented low-cardinality HTTP operational meters for master/files, storage capacity admission and writeability/free-space readiness, plus tests for balanced metric lifecycles, non-consuming low-disk rejection/preserved reads and HTTP admin/cache/role protection while Hub is disabled. These newer changes await combined build/tests; they are not release evidence yet. Worker/importer heartbeat/dependency/scanner readiness is being implemented by the runtime agent.
- Schema34 stable inspection snapshots/global policy, schema35 auxiliary principal transition guards and schema36 audited bounded creator overrides are coordinated in one upcoming checkpoint. Entry-point synthetic fixture passes policy/disk-reserve rendering and invalid-integer rejection; no service redeployment. Admin quota UI/effective creator limits are next frontend integration.
- Base98009345 commits additional tested rollback regression and prepared live scripts; base9ae45475 adds authoritative Warden Hub wiki guide. Appfa98c42 adds scoped wiki synchronization with per-file provenance, rejecting unsafe/missing/uncommitted selections and checking drift while preserving unrelated content. Scoped sync/check and full navigation/source coverage passed0problems (46synced/11authored/62pages). Full sync originally could not run because this source snapshot lacks an unrelated vehicle weapon catalog; no fabricated source or unrelated overwrite.
- Isolated restart launch approval remains pending. No launch retry, production action or unrelated-session change. Goal remains active; readiness/policy/isolation/browser/provider/game acceptance still incomplete.

## Combined build and acceptance audit — 11 September 2026

- Combined master Release build passed with zero warnings/errors after schemas34/35/36, quota overrides, service isolation, administrative aggregates, operational meters, storage admission and files/worker/importer readiness were integrated. This confirms compilation only; transaction agent is running the combined Community/admin/isolation/restore regression against the dedicated local MariaDB and scanner.
- App a38bb5e adds audited administrator quota controls and effective creator limits. npm check and production build passed368pages. No actual Next browser acceptance is claimed. The frontend acceptance audit found missing draft deletion (required by the plan) plus smaller appeal/media states; fixes and safe draft lifecycle work are assigned rather than removed from scope.
- Existing pinned files/worker/importer images contain the required healthcheck utilities under UID1654/read-only/no-network constraints. The HTTP status predicate accepts200/rejects503. Sanitized artifacts/hub-readiness/utilities.json records image identities; these older images do not prove the new readiness endpoints. Current-image endpoint and dependency-outage/recovery validation remains outstanding.
- Added ops/community/OPERATIONS.md with the daily readiness, queue, quota, moderation, outbox, capacity and paired-backup checks. It distinguishes in-process instruments from an installed collector/alerting backend and records missing rejection-type/storage gauges for follow-up.
- Three subagents continue independently on regression/isolation/restore, readiness/telemetry and frontend acceptance. Pending approval for the previously rejected isolated restart launch remains unanswered; no retry or alternative launch. Real browser/GitHub/game/restart and final release-candidate gates remain open. No production changes; goal active and incomplete.

## Committed security/readiness checkpoint and release inventory — 11 September 2026

- Previous goal turn made concrete progress: combined compilation, schema36 restore evidence and frontend quota UI were recorded. Current combined Community/admin regression completed182tests:181passed,1failed,0ignored (community-isolation-policy-operations-full.trx). The failing new attack fixture supplied an invalid empty installation guide before reaching its attack assertion; corrected to a valid guide. Final clean Release build passed0warnings/errors and all4 affected isolation/grants/restore tests passed (community-isolation-final.trx,11.09s). Do not describe this as an independently rerun full182 pass.
- Master1a9dd9b atomically commits schema34 immutable inspection policy, schema35 auxiliary-principal guards, schema36 audited account quota overrides, administrative aggregates, operational meters, storage admission and cached service readiness with tests/config/docs. Final real paired restore evidence artifacts/hub-restore-drill/isolation-final-dabb110a7ecc44189048b4a563cb353b.json verifies schema36 guard definitions/DEFINER/registry, denial of restored-worker published-manifest injection, private HTTP package/media delivery and pending image recovery.
- Master835d899 adds read-only source/image/evidence inventory recording and RELEASE-INVENTORY.md. PowerShell parse and real development snapshot passed; dirty candidate mode correctly rejected before image processing. Evidence artifacts/hub-release/206e2c67cb7e49759813a41e4f085f5a/manifest.json is explicitly a development snapshot of then-current sources and older image evidence, not a release candidate. The recorder always leaves releaseReadinessProven false; evidence hashes do not substitute for H00-H15 review.
- Follow-up ownership: launcher agent implements schema37 safe owner/revision-fenced unpublished-draft deletion and frontend draft preservation; runtime agent adds bounded validation/storage telemetry and actual collector procedure; transaction agent adds startup detection of missing/tampered service guards. The explicit CommunityPolicy section omitted from master.example.json is being added. These are remaining required work, not deferred features.
- Local ignored dotnet-counters10.0.745401 installed without changing global tools or tracked package configuration; actual new-meter collection awaits updated binaries. Existing image utility checks do not establish new endpoint/outage behavior. Pending isolated restart approval remains unanswered; no rejected launch retry, production publication/deployment or unrelated-session modification. Goal active and incomplete.

## Latest-main compatibility and current acceptance status — 11 September 2026

- Previous turn made progress with committed schema36 security/readiness and reproducible release inventory. Read-only fetch refreshed master origin/main to98837f57ae19a85d439657cfaa9394996643af35. Its schema remains11; the two incoming commits change release notes, package README and version.json only. Merge004571d integrates those changes into the dedicated Hub branch without conflicts or overwriting active edits; version metadata now tracks upstream build56. This is source integration, not deployment or proof of rebuilt artifact compatibility.
- App6f88d6c fixes loss of appeal/profile/reply drafts across collapse, token refresh/logout and comment revision refresh; preserves the original comment edit revision; adds explicit edit-cancel confirmation, bounded creation timeout and missing-media retry. Agent reports final npm check and production build368pages passed. Actual browser journeys remain unverified.
- Updated the top-level acceptance/next-work section to reflect completed interrupted transport, controlled load, schema36 isolation/restore and outstanding draft deletion/current-service/browser/provider/game work. Every H00-H15 checkbox remains open until its whole gate has direct evidence.
- Runtime telemetry additions are ready for coordinated validation; schema37 deletion and master startup guard checks are under implementation. No production publication/deployment, blocked launch retry or unrelated-session changes. Goal remains active.
