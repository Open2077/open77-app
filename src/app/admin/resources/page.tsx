import { CommunityReviewPanel } from "@/components/admin/community-review-panel";

export const metadata = { title: "Workshop review queue", robots: { index: false, follow: false } };
export default function ResourcesReviewPage() { return <CommunityReviewPanel />; }
