import { CommunityReportsPanel } from "@/components/admin/community-reports-panel";

export const metadata = { title: "Workshop reports", robots: { index: false, follow: false } };
export default function ReportsPage() { return <CommunityReportsPanel />; }
